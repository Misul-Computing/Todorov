# project_plan

status: current (as of 2026-06-02).

this file is the canonical, persistent project state. any agent picking up the project starts here. updating this file is a hard rule with prosecutor enforcement (see `CLAUDE.md` and the rule section at the bottom of this file).

last updated: 2026-05-14 (the compression-first 100k local path still has a local full small nm candidate. the current bounded exact qa adapter product is `local_100k_source_subtoken_qa_adapter`: it stores a source-subtoken compressed source-heldout qa payload inside torch module state, answers 4,096 bounded paraphrased questions exactly, passes transformer/recurrent host probes, false-hit controls, random-label collapse, and trained recompression-update controls, reaches 24.19976921301639x adapter strict multiplier and 23.847532408460314x paper-surface multiplier, and beats raw content scan plus raw undercharged mph diagnostics. it is not a static compression breakthrough because the fair same-subtoken content scan remains slightly stronger at 24.20535549399815x. the current delta-update product is `local_100k_source_subtoken_delta_update_adapter`: it stores arbitrary replacement bytes for 512 changed facts in a charged 138,104 bit varint-delta-offset patch stream, answers updated and unchanged facts exactly after `state_dict` reload, beats full updated-adapter recompression as the same-block content-scan update baseline by 242,448 delta bits, beats an undercharged mph update table by 50,312 bits, reports an equal-cost matched delta-patch content scan, and keeps total static compression authorization at 0.0 because base-plus-delta loses to full recompression by 1,992 bits. the current narrow source-code byte-compression product is `local_100k_source_subtoken_structure_block_codec`: it reconstructs 99,761 held-out source bytes exactly from charged count-delta, body-subtoken, and dictionary streams in reloadable torch module state, lowers charged payload bits from the prior source-token product's 123,088 to 120,952, improves strict charged bits over the best same-block standard codec by 0.048648916163515785, and keeps random-label payload gain negative. the broader source-code corpus product is now `local_100k_source_subtoken_global_stream_corpus_codec`: it reconstructs five frozen-manifest source-code blocks exactly with one charged 256-token shared dictionary plus charged global count, body, and length/framing streams in torch module state, reloads exactly from `state_dict`, reduces aggregate selected payload bits from the prior shared-dictionary corpus product's 803,400 to 699,144, beats the stronger global raw standard baseline at 736,504 bits by 37,360 bits, and raises aggregate payload improvement over the same-block standard sweep to 0.1772375940274339 with random-label and disabled controls passing. a new public context-mixing pressure audit shows paq8px v214 level 2 compresses the same hard raw joined source to 50,712 bytes, or 405,696 payload bits, so the source-code byte-compression line is not competitor-beating against the strongest checked public compressor. the current narrow source-code exact-retrieval product is `local_100k_source_subtoken_disjoint_retrieval_codec`: it retrieves 14,715 aligned 32-byte chunks exactly from disjoint frozen source blocks after torch `state_dict` reload, uses zero train path/hash overlap, charges 431,536 retrieval bits, beats executable raw content scan by 19,592 bits, and beats an undercharged mph diagnostic by 19,608 bits, but it must now be compared against paq8px payload pressure as well. the current source-authored relation result is `local_100k_source_authored_relation_diagnostic`: it answers 337 authored definition/import relation queries exactly and shows read-work gain, but loses to an honest relation mph/index by 132,664 bits and does not beat a fair unlimited relation-aware scanner, so product authorization stays 0.0. `local_100k_source_dense_authored_relation_diagnostic` answers 3,741 authored relation queries after removing an answer-contained signature-query leak, beats honest mph/index, but remains a diagnostic because paq8px and the fair scanner are stronger. `local_100k_zstd_trained_dictionary_baseline_audit` shows that charged train-only zstd dictionaries lose to the current source-subtoken block payload by 30,424 bits and to the current global-stream frozen-corpus payload by 283,696 bits, while even the undercharged payload-only dictionary diagnostic loses by 26,248 bits on the block and 250,848 bits on the corpus; zstd is no longer the strongest public compression pressure line. the new narrow source-authored relation-index product is `local_100k_source_relation_mph_codec`: it answers 3,741 keyed authored relation queries exactly from charged module state, uses 17-bit fingerprints, charges 248,784 relation bits, recomputes the paq8px level 2 relation baseline in-run at 252,952 payload bits plus 8,192 decoder bits and beats it by 12,360 bits, beats raw-source paq content scan by 165,104 bits, beats undercharged and honest mph relation diagnostics, and keeps random-label rebuild-density, disabled, shuffled-fingerprint, and hardened wrong-query controls collapsed. it is not a static breakthrough because the operation remains a narrow keyed relation index. strict 600x authorization, static breakthrough authorization, broad high-density knowledge authorization, arbitrary chat authorization, and full nm authorization remain 0.0. these are tested local bounded compression products and audits, not paid-scale trainability, not biological neuron-density proof, not strict 600x breakthrough proof, and not external-simulator grounding.)

current compression update (2026-05-14): `local_100k_external_relation_adapter` is now the current public-corpus llm-adoptable relation adapter product. it answers `6247` exact source-authored relation facts from pinned cpython `v3.12.3` files, charges `413600` selected relation bits plus a `4096` bit model-package header, reaches `35.65344680851064x` strict multiplier and `35.303822875967214x` model-package strict multiplier, packages the payload inside tiny transformer-style, recurrent/state-style, and state-space-style hosts, recomputes paq8px v214 level 2 source-scan pressure at `606072` accounted bits, beats that source-scan line by `192472` bits, beats honest mph relation indexing by `3098120` bits, and keeps true base-weight implicit storage, static breakthrough, strict 600x, broad knowledge, arbitrary chat, and full nm authorization at `0.0`.

current paper-facing compression update (2026-05-14): `local_100k_model_state_knowledge_pack` is now the broader bounded knowledge-pack product. it answers `9754` exact authored relation facts from pinned cpython `v3.12.3` source, documentation, and config surfaces, charges `679400` selected relation bits plus an `8192` bit model-package header, reaches `37.6093164556962x` strict multiplier and `37.1612374780393x` model-package strict multiplier, packages the payload inside tiny transformer-style, recurrent/state-style, and state-space-style hosts, recomputes paq8px v214 level 2 source-scan pressure at `1385264` accounted bits, beats that strongest checked line by `705864` bits, beats zstd level 19 source scan by `1515616` bits, beats honest mph indexing by `5194024` bits, beats product-key-style storage, rag/knn storage, lora-style exact payload lower bound, and model-edit exact payload lower bound, and passes adapter export, recompress update, rollback, and control gates. base-weight implicit storage, broad breakthrough, strict 600x, broad open-domain knowledge, arbitrary chat, and full nm authorization remain `0.0`.

current architecture-track update (2026-06-02): deyan reactivated the old todorov architecture track for a one-session v0.1, overriding the no-paid-compute state, on a personal runpod h200. a clean test-gated descent-memory codebase was built from scratch (git branch `v0.1`, dir `v01/`) and a controlled toy comparison was run. the recurrent descent memory (candidate E of `wiki/synthesis/substrate_requires_architectural_change.md`) was made numerically stable but learns at chance on mqar; the linear delta control is also at chance; a 4-layer attention model solves passkey/induction at exact_acc 1.000 (n=100, wilson 95% lower bound 0.963). run card `wiki/tests/v0_1_descent_memory_toy_results.md`, synthesis `wiki/synthesis/descent_memory_intervention.md`, bugs `wiki/mistakes/descent_memory_v0_1_bugs.md`. this does not change the canonical compression-lane workstream below; it records a user-authorized architecture side-session and its honest negative-on-memory / positive-on-attention result.

## master plan: neural model program (2026-04-26)

this is the single canonical session-start plan. all other status files are subordinate summaries or specialized protocols. when another plan file disagrees with this one, fix this file first, then sync the subordinate file only if the current task touches that surface.

### linux migration handoff

the linux migration handoff is `docs/linux_handoff_2026-05-02.md`. it records the windows-to-linux transfer state, current validation commands, user preferences, mistake history, compression-mirror metrics, and next local work. it is an operational supplement only; this file remains the canonical project plan and the first source to repair when state files disagree.

### project identity

the active scientific object is the **neural model**. the old todorov architecture, prior names, and paid runs are historical evidence about failed substrates and trainability. they are not the live design identity.

the target is a **3d latent world-memory model**: a neural model that learns local state, memory, compression, replay, imagination, physics reasoning, language answers, and discrete actions in embodied worlds. language is a co-primary training and output surface, but not the sole objective. early observations should include exact world and physics state plus world-grounded language. early outputs are language plus actions. drawing and image-generation output are not early targets.

the first embodied task family is object permanence, occlusion, delayed use, simple dynamics, and counterfactual physics queries. the current local bridge is deterministic synthetic exact-state 3d; the external simulator is not chosen yet.

### flagship paper thesis

the flagship paper argues, conditionally, that a neural world-memory model can become scientifically coherent only if memory, imagination, replay, and world state are treated as operation-preserving memory objects with proof-gated compression, trainability controls, and exact-state evaluation.

this is not proved. every novelty claim remains conditional until the required oracle, symbolic, trainable, and related-work gates pass.

### six-lane portfolio

1. [[neural_model_lane_cellular_state_storage]] — biology-led local state storage: cellular, molecular, membrane, subthreshold, compartment, eligibility, and gating operations translated into candidate mathematical state.
2. [[neural_model_lane_operation_preserving_compression]] — oracle compression bounds, schema/residual codes, imagined-branch program bits, and task-relative rate-distortion.
3. [[neural_model_lane_memory_replay_imagination]] — write/read routing, interference, replay rewrite, and imagination as latent rollout and recombination.
4. [[neural_model_lane_3d_world_physics]] — exact-state embodied 3d worlds, physics variables, world-grounded language, discrete actions, and simulator selection.
5. [[neural_model_lane_trainability_evaluation]] — hard symbolic gates, oracle controls, tiny trainable mirror, telemetry, confidence intervals, and failure localization.
6. [[neural_model_lane_project_operations]] — single-plan governance, private-until-proved policy, prosecutor requirements, curriculum support, archive policy, and paper registry.

### ranked unknowns

1. how biological local state stores useful information over time.
2. what oracle compression ratios are possible on hard symbolic worlds.
3. what compression operations preserve memory, replay, imagination, and world-state operations rather than tensors.
4. what simulator can provide exact-state embodied 3d physics without unnecessary from-scratch work.
5. what trainability controls localize learned-write, learned-read, gate, address, and decoder failure.
6. which side mechanisms are paper-worthy and which are only engineering choices.

### side-paper policy

side projects become active only as gated proof packages. each candidate must define:

- mathematical operation
- prior-art boundary
- preserved or improved operation
- source or proof basis
- symbolic or oracle bound
- trainable test
- controls
- telemetry
- kill condition
- paper claim if proved

initial side-paper seeds are operation-preserving compression stack, hard symbolic neural-model test material, cellular/local state storage, replay and imagination compression, and trainability failure/control methodology. none is a claim of novelty until proved.

### archive policy

do not move old files in this update. old architecture material remains historical evidence until a focused migration pass creates `neuroloc/wiki/archive/` subfolders and updates `OPERATING_DIRECTIVE.md` to govern archive lifecycle. run cards and mistake docs stay preserved as evidence. old architecture execution is historical context, not active identity.

### simulator-selection gate

do not choose a simulator yet. candidate simulators must be evaluated for customizable embodied 3d worlds, deterministic runs, exact hidden state, physics variables, object permanence and occlusion tasks, discrete actions, world-grounded language generation, cheap local or cpu validation where possible, and exportable episode contracts compatible with the hard symbolic tests.

### paid-compute gate

paid compute remains blocked until broad lane research, mechanism dossiers, dossier-driven test material, oracle compression bounds where relevant, a tiny trainable mirror, cpu controls, telemetry, prosecutor-clean docs, and one explicitly selected hypothesis exist. paid compute is validation after proof gates, not exploration.

### related-work boundary: content-routed sparse reads

the 2026 subq / selective sparse attention public material is now recorded in [[content_routed_sparse_read_prior]]. it validates the project's insistence that long memory depends on content-dependent routing and functional context, and it lets the project demote fixed position-only sparse patterns to controls for arbitrary-position memory. it does not validate the project's stronger memory-object compression claim.

future compression claims must beat or complement a content-routed sparse-read baseline over verbatim context wherever that baseline is fair. this is a stricter comparison standard, not a new paid-compute path.

the first such baseline is now implemented in [[tests/compression_under_bit_budget_mirror]]. it solves the repaired `compression_under_bit_budget` smoke task by selecting the committed legal source event and its next-source velocity support event. a matched-budget sparse-read control and distributed-evidence probe are also implemented. the result demotes the original source-pair mirror as compression evidence: it is a useful source-selection diagnostic, but the next proof target must be a learned compact code that preserves operations on distributed evidence under the compact bit budget.

### related-work pressure matrix

the 2026-05-06 multi-lane research pass is consolidated in [[neural_model_related_work_pressure_matrix]]. it covers cellular/local state storage, operation-preserving compression, memory/addressing/replay/imagination, 3d world simulators, trainability/evaluation, and content-routed sparse-read baselines. the main result is a tighter prior-art boundary: local state, fast weights, differentiable memory, sparse attention, semantic compression, context compression, replay, world models, and oracle-style evaluations are all heavily covered. the project remains defensible only as a compound proof of operation-preserving learned memory objects under exact controls.

### local 100k model path, compression first

the next phase is local implementation and simulation, not paid compute or full-model integration. deyan explicitly overrode the 2026-05-08 planning-only blocker on 2026-05-09, then raised the active parameter target from under 10k to 100k for coherence. the active deliverable is an actual 100k-parameter local model path whose compression surface is solved first under controls.

the model work must start from the wiki evidence and current code. compression is first because useful-state density is the load-bearing claim. a candidate can be promoted only if it beats fair sparse-read and verbatim baselines where those are applicable, reports accounted bits, survives factor-heldout and parser-resistant gates, and shows the intended learned path is used rather than record lookup.

the 100k target must preserve the full foundation stack as mechanisms rather than labels: learned local state variables, context state, working memory, episodic memory, long-term memory, write dynamics, read dynamics, replay or rewrite, latent rollout or recombination, action decoding, language-answer decoding, training objective, controls, telemetry, and kill conditions.

imagination and replay remain first-class proof obligations. imagination means latent rollout or recombination with dynamics, uncertainty, provenance, and measurable operation value. replay means delayed reactivation or rewrite that changes future recall, compression, interference, or action success. lookup, record rendering, and branch copying remain baselines unless the mechanism-specific controls prove otherwise.

the local v1, local 10k, and local foundation artifacts may be used as scaffolds, regression surfaces, and plumbing references. they are not enough as final evidence by themselves. the new work must repair the compression and learned-state path first, then integrate only the parts that survive controls.

the first 100k-path component result is [[tests/local_state_write_read_mirror]]. it composes the typed event-binding bridge with a separate learned recurrent compact-state cell. registered smoke validation across four factor-heldout axes reaches minimum joint/state/action success 0.96875, update success 1.0, 37,719 maximum parameters, 56 accounted bits, matched-budget sparse-read success 0.0, zero-state success 0.0, shuffled-state success 0.0, no-update success 0.0, random-update success 0.0, and engineering pass 1.0. this is accepted only as learned local state write/read/update evidence.

the first finished local 100k model candidate on the symbolic-language surface is now [[tests/local_100k_replay_answer_mirror]]. it adds delayed compact-code reactivation, compact rewrite, bounded answer decoding, and learned branch rollout. registered smoke validation across four factor-heldout axes reaches 89,877 maximum parameters, 56 accounted bits, 0.96875 minimum initial joint/state/action success, 0.96875 targeted replay success, 0.96875 rewrite success, 1.0 branch rollout success, matched-budget sparse-read success 0.0, no-replay success 0.0, recency replay success 0.0, matched-compute dummy replay success 0.0, decoder-disabled success 0.0, no-branch success 0.0, wrong-branch success 0.0, random-branch success 0.0, hard-case branch gain 1.0, and engineering pass 1.0. registered hard validation now passes after repairing a lossy generated-language number vocabulary; hard reaches 98,817 maximum parameters, 60 accounted bits, 1.0 initial joint success, 1.0 targeted replay success, 0.96875 rewrite success, 0.984375 branch rollout success, and engineering pass 1.0. this remains useful as the symbolic-language predecessor to the exact-state 3d candidate.

the current top local result is [[tests/local_100k_full_nm]]. it replaces the component-style exact-state 3d mirror with one trainable `torch.nn.Module`: shared encoder, learned binary bottleneck, recurrent internal state, replay, rewrite, learned branch transition, action decoding, provenance decoding, and bounded answer decoding. registered hard validation passes on the deterministic exact-state 3d bridge with 81,070 maximum parameters, 24 learned latent bits, 20 fixed bridge/schema/answer bits, 44 accounted bits, 1.0 initial world-state joint success, 1.0 object permanence success, 1.0 occluded localization success, 1.0 action consequence success, 1.0 targeted replay success, 1.0 rewrite success, 1.0 learned branch transition success, 1.0 bounded language-answer success, useful density advantage over the 51-bit 3d baseline of 0.003119429590017826, and zero success for no-memory, code-disabled, shuffled-code, decoder-disabled, no-replay, random-replay, no-branch, wrong-branch, no-integration, and wrong-dynamics controls. this promotes only to a local full small nm candidate within the synthetic local scope: no broad full-model authorization, arbitrary chat, external simulator transfer, paid-scale trainability, or open-ended world modeling is authorized.

the first high-density neuron-cell proof surface is [[tests/local_100k_high_density_cell]]. it implements a bounded hybrid associative cell with a tiny trainable control/read module and explicit committed-state accounting. registered smoke and hard validation pass as an engineering artifact: hard stores 4,096 exact associative facts with 8 trainable parameters, 114,688 useful retrievable bits, 118,816 committed state bits, exact retrieval success 1.0, params-only density 14,336 useful bits per parameter-equivalent, and params-only multiplier 5,734.4x against the 2.5-bit ordinary factual baseline. strict accounting reaches only 15.427495291902071 useful bits per parameter-equivalent, or 6.1709981167608285x, so the strict 600x breakthrough claim is rejected. [[mistakes/local_100k_high_density_cell_strict_600x_not_met]] records the roadblock. the result is a params-only partial high-density cell candidate, not full nm completion and not a publishable strict compression breakthrough.

the schema-density attempt [[tests/local_100k_schema_density_cell]] is demoted by [[mistakes/schema_density_cell_structured_target_category_error]]. it changed the target from independent or unknown-structure knowledge to structured schema-generated associative facts, which made the answer recoverable from planned structure. although its registered smoke and hard validation still demonstrate formula compression and useful boundary telemetry, it does not authorize the requested high-density knowledge-compression claim. [[schema_density_cell_boundary]] is now historical context only.

the unstructured exact-fact probe [[tests/local_100k_unstructured_density_cell]] records the hard entropy boundary. registered smoke and hard validation pass as a negative result: hard uses 4,096 random-label exact facts, 122,880 useful retrievable bits, 1,218 committed state bits under the 600x budget, target state budget 1,246.72 bits, entropy lower bound 122,880 bits, entropy budget gap 121,633.28 bits, exact retrieval success 0.0, information-theoretic 600x possible 0.0, strict 600x pass 0.0, and breakthrough authorization 0.0. [[mistakes/unstructured_exact_600x_entropy_wall]] records that exact independent labels cannot be compressed by 600x without a hidden side channel. the only remaining plausible path is unknown-structure data with learned redundancy and a random-label twin control.

the unknown-structure corpus probe [[tests/local_100k_unknown_structure_density_probe]] sets the first non-generated real-data baseline after the structured-target error. registered smoke and hard validation pass as a boundary result: hard retrieves 4,096 unique 32-byte wiki corpus chunks exactly, collapses the random-label twin to 0.0, charges 481,282 committed bits for compressed payload, decoder, and manifest, and reaches strict multiplier 13.941917871967359x. this proves real corpus redundancy exists, but not enough for the 600x claim under the current standard-codec offset-key setup. the next target is a learned unknown-structure associative cell that beats this charged baseline.

the learned unknown-structure residual cell [[tests/local_100k_learned_unknown_structure_density_cell]] is a registered hard defeat. it moves to source-heldout non-generated corpus chunks, opaque associative keys, a learned byte-phrase dictionary, charged residual streams, exact value/provenance decoding, and the required random-label and disabled-path controls. hard validation reaches exact retrieval success 1.0, heldout exact retrieval success 1.0, cross-label random scoring 0.0, and controls collapse 1.0, but a separately built random-label twin stores at 1.0. strict multiplier is only 3.0525410753623334x, selected standard-codec multiplier is 5.029465628030584x, and the prior charged corpus-codec baseline remains stronger at 13.941917871967359x. because exact retrieval still depends on per-fact residual/key rows, [[mistakes/learned_unknown_structure_residual_table_defeat]] blocks promotion to a high-density knowledge-compression cell.

the compression research corpus at `neuroloc/compression/` is the current recovery product for the next attempt. it consolidates the in-repo attempt history, modern test-time-memory and sparse-memory related work, open-source and passion-project baselines, theory limits, hidden-table tests, the required baseline ladder, and the first buildable non-row candidate. the first implementation of that line is [[tests/local_100k_shared_predictor_exact_codec]]. it is a tested exact-codec product: one block value stream, no per-fact value slices, source-heldout opaque keys, random-label payload-cost control, and registered smoke/hard validation. it does not solve the high-density target because strict accounting reaches only 4.352614012398447x and loses to the charged corpus-codec and mph-payload baselines.

the first source/offset compression product is [[tests/local_100k_source_block_codec]]. it changes the retrieval contract from arbitrary opaque associative keys to source id plus offset, then compresses the source-heldout test corpus as one block stream. registered smoke and hard validation pass with exact retrieval success 1.0, random-label twin success 0.0, controls collapse 1.0, 476,958 strict accounted bits, 1,048,576 useful retrievable bits, and strict multiplier 14.06876726917481x. this beats the prior charged corpus-codec strict multiplier 13.941917871967359x, but it is a source/offset codec product, not high-density arbitrary associative knowledge memory. [[mistakes/source_block_codec_raw_cache_category_error]] records the raw-cache false-compression path that was found during implementation and fixed before accepting the product.

the next compression product is [[tests/local_100k_content_addressed_source_codec]]. it keeps one compressed source block but removes visible source/offset query fields. each query supplies a bounded opaque content digest handle; the read path decompresses the charged block stream, scans candidate windows, computes the digest, and returns the exact matching chunk and provenance. registered smoke and hard validation pass with exact retrieval success 1.0, random-label twin success 0.0, controls collapse 1.0, selected digest collision count 0.0, source-offset routing 0.0, key assignment bits 0.0, 476,954 strict accounted bits, 1,048,576 useful retrievable bits, and strict multiplier 14.06888524576417x. this slightly beats the source-block strict multiplier 14.06876726917481x, but it is still a content-handle codec, not arbitrary associative knowledge memory.

the current bounded llm-facing compression product is [[tests/local_100k_llm_semantic_qa_codec]]. it keeps one compressed source block and changes the query surface to natural-language questions over token-signature evidence. the stored test facts expose no source id, offset, content digest, answer digest, assignment key, or per-fact answer row; the read path normalizes the runtime question, computes a token-signature handle, decompresses the charged block stream, scans candidate windows, and returns the exact following 32-byte answer plus provenance. registered smoke and hard validation pass with exact answer success 1.0, random-label twin success 0.0, controls collapse 1.0, selected semantic collision count 0.0, source-offset routing 0.0, content-digest key target 0.0, assignment row count 0.0, per-fact value row count 0.0, raw source block retained 0.0, 476,938 strict accounted bits, 1,048,576 useful retrievable bits, and strict multiplier 14.06935717190861x. this slightly beats the content-addressed product but still loses to the minimal-perfect-hash payload baseline at 14.070360120095941x and is not learned semantic recall.

the current weight-carried bounded compression product is [[tests/local_100k_weight_carried_qa_codec]]. it keeps the bounded natural-language qa surface but carries the compressed source-heldout payload inside torch module state as an adapter buffer, removes the stored manifest, charges a 40-bit model header and 32,768-bit parser/decoder budget, and tests a decompression-recompression update path for later finetuning or post-training integration. registered smoke and hard validation pass with exact answer success 1.0, random-label twin success 0.0, controls collapse 1.0, selected semantic collision count 0.0, external payload store 0.0, stored manifest 0.0, assignment row count 0.0, per-fact value row count 0.0, adapter recompression update success 1.0, adapter state-dict reload success 1.0, 441,064 strict accounted bits, 1,048,576 useful retrievable bits, strict multiplier 15.215221373768888x, beats llm-facing qa baseline 1.0, and beats matched mph payload baseline 1.0. this crosses the requested 15x product line but is still a token-signature exact codec, not learned semantic recall, not implicit base-weight storage, not arbitrary chat, and not a 600x proof.

the current paper-facing bounded compression product is [[tests/local_100k_margin_recompression_adapter]]. it preserves the model-state adapter payload, repairs the prior source-holdout overlap found in [[mistakes/paper_ready_adapter_source_holdout_overlap]], and keeps the five paper-facing requirements: transformer host integration, recurrent/state-style host integration, prior public-baseline comparison, multi-domain source-heldout corpus slices, and paraphrase-stable bounded qa plus trained adapter recompression update. registered smoke and hard validation pass with exact answer success 1.0, paraphrase-stable answer success 1.0, controls collapse 1.0, false-hit rates 0.0 for wrong, unanswerable, partial-overlap, and marker-injection queries, transformer surface pass 1.0, recurrent surface pass 1.0, static public baseline pass 1.0, source holdout pass 1.0, large-margin-over-mph pass 1.0, trainable recompression update success 1.0, adapter state-dict reload success 1.0, 295,144 committed state bits, 299,240 paper-surface accounted bits, 1,048,576 useful retrievable bits, adapter strict multiplier 22.732738950163952x, paper-surface strict multiplier 22.421639537059313x, and paper-ready requirement count 5.0. this is the current presentable local compression product and paper-ready evidence package, but strict breakthrough authorization remains 0.0 because the query path is still token-signature based, base-weight implicit storage is not proved, and the executable same-block content-scan diagnostic is not beaten.

the semantic-alias diagnostic [[tests/local_100k_semantic_alias_payload_adapter]] is not promoted. registered smoke and hard validation pass as an engineering diagnostic with 4,096 exact answers, 258,144 block payload bits, 290,952 committed state bits, 295,048 paper-surface accounted bits, 23.06526987269378x adapter strict multiplier, and 22.7450665654402x paper-surface multiplier. it is demoted by [[mistakes/semantic_alias_payload_adapter_formula_alias_category_error]] because the alias question labels are generated from source anchors and a fair same-interface alias content scan reaches 1.0 exact success. publishable breakthrough authorization and strict breakthrough authorization remain 0.0.

the source-native relation diagnostic [[tests/local_100k_source_native_relation_adapter]] is not promoted. registered hard validation passes as an engineering diagnostic with 4,096 exact answers, 258,144 block payload bits, 290,960 committed state bits, 295,056 paper-surface accounted bits, 23.064635688754468x adapter strict multiplier, and 22.744449867143864x paper-surface multiplier. it is demoted by [[mistakes/source_native_relation_stride_rule_category_error]] because the relation is fixed stride arithmetic over the source block and a fair stride-aware content scan reaches 1.0 exact success. publishable relation breakthrough authorization and strict breakthrough authorization remain 0.0.

the prior exact-state 3d result [[tests/local_100k_3d_nm_mirror]] remains the baseline that the full local nm beats on compression. it reached 66,559 maximum parameters and 51 accounted bits with an explicit compact-state path and exact transition over decoded compact state. it is no longer the top local result, but remains the comparison surface for exact-state 3d operation preservation.

future work must follow the researcher workflow: record tests run, exact results, mistakes, roadblocks, decisions, and evidence locations in the owning wiki records. roadblocks, novel mechanisms, ambiguous failures, and scientific uncertainty require focused research or review agents before implementation continues. out-of-the-box ideas are required, but each must be grounded in math, biology or ml evidence, a falsifiable test, controls, telemetry, and a kill condition.

## teaching curriculum package (2026-04-17; preserved after 2026-04-23)

**the teaching PDF curriculum remains preserved and reviewable** in the plan file at `~/.claude/plans/compressed-dancing-haven.md`. after the 2026-04-26 master-plan decision, the current scientific planning lane is the neural-model master-plan research phase, not immediate curriculum production and not architecture execution. paid compute is paused indefinitely. the architectural-intervention track (A-E candidates in `wiki/synthesis/substrate_requires_architectural_change.md`) remains in the research backlog but is NOT the active workstream — it becomes active again only after lane research, dossier-driven test material, oracle compression bounds where relevant, cpu controls, and a selected intervention exist. any agent picking up this project reads this file as the current state before treating older curriculum or architecture plans as active.

the curriculum specifies 36 chapters across 6 phases that teach a reader from near-zero math and biology prerequisites up through machine-learning math, advanced neuroscience, and paper-implementation competence. each chapter is a 20-25 page PDF compiled from LaTeX, produced in strict sequence with per-chapter outline approval, parallel research agents, prosecutor validation on both research and draft, and user review-revision cycle before finalization. chapter details at `## curriculum track` below.

**while working on curriculum material, no paid compute run is authorised.** the 2026-04-26 neural-model master plan is also no-paid-compute. paid work returns only after broad lane research, mechanism dossiers, dossier-driven test material, oracle compression analysis where relevant, a tiny trainable mirror, cpu controls, telemetry, prosecutor-clean docs, and a single explicit hypothesis exist.

## status

**current run**: none. pod stopped 2026-04-17 by user after the SIXTH paid run (run3_cognition_phase1) trained to val_bpb 6.3519 on the synthetic cognition corpus and produced 0/100 passkey at 256 and 1024 in partial eval. **SIX paid runs total (god_run, god_run_v2, run1_baseline_noerasure, run2_slot_memory, run2_slot_memory_retention_fixed, run3_cognition_phase1) have produced 0% passkey at 256 tokens** across two substrates (matrix and slot), two retention regimes (broken inherited and fixed alpha_log_mean=5.0), and two corpora (fineweb-edu natural text and a synthetic cognition corpus designed to require retrieval). the cognition corpus was the project's cleanest test of the slot substrate — retrieval is an explicit 50% fraction of the training blocks — and the substrate still did not learn it. val_bpb on the cognition run plateaued at the alphabet prior from step 150 and never improved. the prior diagnosis (`training_objective_vs_architectural_goal.md`, 2026-04-16) predicted this run would discriminate between "the training objective is the problem" and "the architecture cannot be trained"; the architecture-cannot-be-trained branch fires. diagnosis is now that the slot substrate needs architectural intervention. any future paid-resume path requires at least one of: open output gate init, auxiliary retrieval loss, orthogonal prototype key init, warm-start from hand-placed addresses, or substrate replacement; after the 2026-04-26 master-plan decision it is additionally blocked behind lane research, mechanism dossiers, dossier-driven test material, oracle compression bounds where relevant, and a tiny mirror. see `wiki/synthesis/substrate_requires_architectural_change.md` for the full analysis and ranked candidate list. run card at `wiki/tests/run3_cognition_phase1_results.md`.

![paid-run retrieval summary](assets/statistics/s1_paid_run_retrieval_results.svg)

**theoretical diagnosis** (see `wiki/synthesis/linear_attention_retrieval_wall.md`): four stacked failure modes, any one of which prevents passkey:
1. classical outer-product capacity ceiling at d_head=64 is ~9 patterns (`0.14 * d`, ramsauer et al. 2021 proved softmax is structurally required for exponential capacity)
2. learned keys under next-byte loss are correlated, not orthogonal (cabannes et al. icml 2024)
3. the default retention `alpha_log_mean=-0.5` gives `alpha_eff=0.377`, which compounds to `10^-109` over 256 tokens — the state literally rounds to zero before retrieval
4. delta layers output noise (structure_ratio 0.977 in god_run_v2) that contaminates the attn layers' residual stream via unconditional residual addition

**pivot decision** (2026-04-14): replace the matrix memory with slot-based memory using softmax addressing, surprise-gated writes, and an explicit output gate. design in `wiki/synthesis/slot_memory_design.md`. cpu validation pipeline (three simulations) gates any paid compute. **superseded 2026-04-17**: the slot pivot was executed through three paid runs (run2_slot_memory, run2_slot_memory_retention_fixed, run3_cognition_phase1) and still produced 0% passkey at 256. the next pivot is architectural intervention from the ranked list in `wiki/synthesis/substrate_requires_architectural_change.md`, not a further substrate design iteration without one of those interventions.

**current question** (2026-05-14): the first local full small nm candidate now exists. bounded qa compression has advanced from the margin recompression adapter's 22.421639537059313x paper-surface multiplier, to the source-structure qa adapter's 23.688602733536655x paper-surface multiplier, to the source-subtoken qa adapter's 23.847532408460314x paper-surface multiplier, but the fair same-subtoken content scan is still slightly stronger. `local_100k_source_subtoken_delta_update_adapter` adds the current narrow delta-update product: it stores arbitrary replacement bytes for 512 changed facts in a charged 138,104 bit varint-delta-offset patch stream, beats full updated-adapter recompression as the same-block content-scan update baseline by 242,448 delta bits, beats an undercharged mph update table by 50,312 bits, reports an equal-cost matched delta-patch content scan, and keeps total static compression authorization at 0.0 because base-plus-delta loses to full recompression by 1,992 bits. narrow source-code byte compression has advanced against in-repo standard codecs and zstd trained dictionaries, and the broader frozen-manifest source-code corpus product reaches aggregate payload improvement 0.1772375940274339 with exact `state_dict` reload. however, paq8px v214 level 2 compresses the same hard raw joined source to 50,712 bytes, or 405,696 payload bits, which beats the current 699,144-bit global-stream payload. the disjoint source-code exact-retrieval product retrieves 14,715 aligned 32-byte chunks after `state_dict` reload and beats the implemented raw content scan and undercharged mph diagnostics, but it is not a public-compressor breakthrough while the paq8px pressure line is lower. the source-authored relation diagnostic answers 337 authored definition/import relation queries exactly, but loses to an honest relation mph/index and a fair unlimited relation-aware scanner. the dense source-authored relation diagnostic answers 3,741 authored definition and statement-structure queries exactly after removing the answer-contained signature-query leak documented in [[mistakes/dense_relation_signature_query_leakage]] and beats honest relation mph/index by 2,928,400 bits, but loses to paq8px level 2 by 176,536 bits and the fair unlimited scanner still solves the task. `local_100k_source_relation_mph_codec` converts the dense relation surface into a narrow product: it answers 3,741 keyed authored relation queries exactly from charged module state, charges 248,784 selected relation bits, recomputes and beats the paq8px relation line by 12,360 bits, beats raw-source paq content scan by 165,104 bits, beats undercharged and honest mph diagnostics, and keeps static breakthrough authorization at 0.0 because the operation is a keyed relation index. `local_100k_external_relation_adapter` now extends the relation path to a public cpython corpus and host-packaged llm adapter surface: hard validation answers 6,247 exact relation facts, charges 413,600 selected relation bits, reaches 35.65344680851064x strict multiplier, beats in-run paq8px source scan by 192,472 bits, beats honest mph indexing by 3,098,120 bits, and passes transformer, recurrent, and state-space host reload probes. structured formula compression is invalid, exact independent random-label compression is blocked by entropy, the learned dictionary/residual associative cell falls to 3.0525410753623334x while depending on per-fact residual rows, and exact qa adapters remain bounded by fair same-interface scanners. the active question is now whether this adapter path can become a standard llm update/packaging mechanism through a trained host router, multi-corpus public benchmark, adapter export format, or model-edit comparison without hidden tables, generated labels, parser mismatch, or uncharged decoder state.

**current obstacle** (2026-04-17): six paid runs at 0% passkey across two substrates, two retention regimes, and two corpora. cpu gates A and B verify the slot mechanism retrieves in isolation. sgd on the training pipeline never opens the output gate (mean gate at sigmoid(-4)≈0.018 through run2_slot_memory_retention_fixed, same regime suspected of run3 absent telemetry). the substrate does not fail for any reason that can be fixed by varying the corpus alone or the hyperparameters of the current initialisation alone.

**immediate next action** (2026-05-14): use [[tests/local_100k_source_subtoken_qa_adapter]] as the bounded exact qa adapter baseline, use [[tests/local_100k_source_subtoken_structure_block_codec]] as the current narrow source-code block codec baseline, use [[tests/local_100k_source_subtoken_global_stream_corpus_codec]] as the current broader frozen-manifest source-code corpus codec baseline, use [[tests/local_100k_source_subtoken_disjoint_retrieval_codec]] as the current narrow source-code exact-retrieval baseline, use [[tests/local_100k_zstd_trained_dictionary_baseline_audit]] as the trained-dictionary pressure line, use [[mistakes/public_context_mixing_baseline_missing]] as the current paq8px public context-mixing blocker for source-code byte-compression promotion, use [[tests/local_100k_source_relation_mph_codec]] as the high-multiplier in-repo relation-index product, and use [[tests/local_100k_external_relation_adapter]] as the public-corpus llm-adoptable relation adapter product. treat [[tests/local_100k_source_subtoken_shared_dictionary_corpus_codec]] and [[tests/local_100k_source_subtoken_structure_corpus_codec]] as superseded corpus baselines and [[tests/local_100k_source_token_structure_corpus_codec]] as a diagnostic only, because the token-corpus product authorization is now held at 0.0 after review found live-corpus drift and weak controls. treat [[tests/local_100k_semantic_alias_payload_adapter]], [[tests/local_100k_source_native_relation_adapter]], [[tests/local_100k_weight_mantissa_payload_adapter]], [[tests/local_100k_source_authored_relation_diagnostic]], and [[tests/local_100k_source_dense_authored_relation_diagnostic]] as parser/formula/accounting/index/public-compressor warnings. the next probe should be a standard-adapter export/update lifecycle, a trained bounded-prompt router into the external relation adapter, a multi-corpus public benchmark, or a charged lora/memit-style comparison on the same relation set. do not iterate generated alias labels, fixed stride rules, learned dictionary plus residual-row paths, uncharged train dictionaries, mantissa payload accounting, naive token-gap splits, byte-class splits, simple adaptive predictors, no-key ordinal relation tables, or parser changes that do not give the scanner the same interface. required controls remain train/test key split, concrete path/hash/ngram holdout counts, no formula-generated labels, no seed oracle, no table-backed values, random-label twin and random-label rebuild-density control, shuffled-question/value/provenance, wrong/unanswerable/partial-overlap/marker-injection false-hit controls, write/read/parser/decoder/adapter/update-disabled paths, sparse-read/product-key/minimal-perfect-hash/verbatim baselines, fair same-interface executable content-scan diagnostic, paq8px public context-mixing pressure, lora-style adapter-storage and model-editing baselines where relevant, trained public dictionary baselines where source-code byte compression is claimed, the charged corpus-codec baseline, the source-block baseline, the content-addressed baseline, the llm-facing qa baseline, the weight-carried qa baseline, the paper-ready adapter baseline, the margin adapter baseline, the source-structure qa baseline, the source-subtoken qa baseline, the source-token-structure block baseline, the source-subtoken-structure corpus baseline, the shared-dictionary corpus baseline, the global-stream corpus baseline, the disjoint-retrieval baseline, the dense authored-relation diagnostic, the source relation mph codec product, the external relation adapter product, and explicit accounting for every stored bit. paid compute, h200, kaggle, pod, external simulator integration, arbitrary chat, and larger-scale integration remain out of scope unless deyan explicitly reauthorizes them.

**current answer to that next action** (2026-05-14): [[tests/local_100k_model_state_knowledge_pack]] completes the multi-corpus public benchmark, standard adapter export/reload, recompress update, rollback, and charged lora/model-edit/rag/product-key/storage comparison parts of the next probe. the remaining paper-strengthening options are a trained bounded host router, a second public repository family, and a real lora or model-edit training run on the same relation set. the current product is paper-facing within the bounded exact relation claim, but the non-claims above remain binding.

## hypothesis under test

**hypothesis (prior, CLOSED NEGATIVE 2026-04-14)**: the baseline dense matrix-memory architecture can recover measurable verbatim retrieval once dense keys and values are restored, overwrite subtraction is removed, and the auxiliary mechanisms that corrupted the address space are removed. **FALSIFIED** by run1_baseline_noerasure: val_bpb 1.4499, passkey 0/100. same result as god_run_v2 despite all 5 bundle features removed. theoretical synthesis in `linear_attention_retrieval_wall.md` explains why this outcome was overdetermined.

**hypothesis (prior, CLOSED NEGATIVE 2026-04-17)**: a slot-based memory with softmax addressing, surprise-gated writes, and explicit output gate will pass passkey at 256 tokens on byte-level language at ~350m params, where the current matrix memory substrate cannot. **FALSIFIED** across three paid runs: run2_slot_memory inherited the retention bug (val_bpb 1.5107, 0/100); run2_slot_memory_retention_fixed fixed retention to alpha_log_mean=5.0 and trained cleanly on fineweb-edu (val_bpb 1.4777, 0/100); run3_cognition_phase1 trained the same substrate on a corpus where retrieval is explicit 50% of training blocks (val_bpb plateaued at alphabet prior 6.3519 from step 150, 0/100). three paid tests, zero passkey, no configuration of corpus or retention makes the substrate learn retrieval under sgd.

**hypothesis (paused backlog, 2026-04-21)**: at least one of the five ranked architectural interventions in `wiki/synthesis/substrate_requires_architectural_change.md` may make the retrieval substrate trainable enough to clear a broader phase-1 neural-model battery at cpu scale and then produce non-zero paid retrieval on the synthetic cognition corpus at 355M params, 4000 steps. this is not the current active hypothesis and no intervention has been selected. candidates in rank order of cost-adjusted expected value remain: (A) open output gate at init (gate_init=0, sigmoid(0)=0.5 - attacks the fixed-point-at-closed-gate pathology observed in run2_slot_memory_retention_fixed), (B) auxiliary retrieval loss weighting MARK_QUERY-following positions 10-100x (attacks the 3%-of-tokens-that-matter dilution for byte-level models), (C) orthogonal prototype key init via QR decomposition (attacks one-slot softmax collapse at init), (D) warm start from hand-placed (key, value) pairs (splits the question into read-side and write-side diagnosis), (E) substrate replacement (titans fast-weight MLP, larimar, differentiable kv table with hard attention).

**hypothesis (prior, CLOSED NEGATIVE 2026-06-10, pre-registered 2026-06-08)**: candidate F, affect-gated write. on the descent-memory substrate (candidate E), routing the inner-loop prediction-error magnitude (surprise) into the write gain may move toy associative recall (mqar) off the chance floor where the unmodulated write does not. this is a three-factor / neuromodulatory plasticity gate (`delta_w = eta * f(pre,post) * M`, `M` = surprise) and attacks the write side the v0.1 masked loss left unsupervised; output-side precision weighting is already maxed in v0.1 and still at chance. it is cpu-only and not active: `affect = 0` reproduces v0.1 as the control, a shuffled-surprise arm isolates the surprise content, and the method-required trainability localisers and matched controls gate any promotion. full pre-registration: `wiki/tests/affect_gated_write_cpu_experiment.md`. **FALSIFIED** by the cpu execution of 2026-06-10 (run card frozen, results appended there): true surprise is at chance under the own-history normaliser (two strengths) and under exact budget matching (realised write gain pinned at 1.000), so surprise content is inert on the write side. the shuffled-surprise control, not the treatment, ignited retrieval (mqar exact 0.360 / token 0.785 at 1200 steps, seed 0, n=100; memory-mediated per the no-write baseline, causally clean, exact ~ token^4 internal consistency) — the first sgd-trained non-chance retrieval on a recurrent memory substrate in this project, but seed-sensitive (chance at seed 1 via endogenous budget suppression) and not reproduced by uniform head-iid or token-correlated noise at matched init/data. the anomaly is promoted to candidate G (stochastic write gain, backlog, own pre-registration plus literature check required before any further arms) in `wiki/synthesis/substrate_requires_architectural_change.md`.

**approved research/test-material plan (current, 2026-04-23)**: do not pick an intervention yet. first build the research material that makes intervention testing meaningful. the active no-paid-compute preparation surface is the neural-model dossier set in `wiki/synthesis/neural_model_research_test_material_plan.md`. each mechanism must define its operation, evidence, test material, metrics, controls, telemetry, and kill condition before any metric or model code is accepted.

**falsifiable claim (future cpu phase-1 gate)**: after the dossier and test-material package exists, at least one candidate intervention must produce nontrivial associative recall above matched no-memory / recency-only / shuffled-address controls, degrade gracefully under interference rather than collapsing immediately, improve delayed-use behavior in a partially observable world, and show positive hard-case gain from additional internal iterations. oracle-write / oracle-read and gate-init controls must localise which part of the loop is learned.

**falsifiable claim (future paid gate after cpu pass)**: after the dossier package, tiny trainable mirror, cpu controls, telemetry, prosecutor-clean docs, and selected hypothesis exist, the corresponding paid preset must produce Wilson 95% lower bound above zero on passkey @ 256 over 100 trials in the run3_cognition_phase1 eval protocol, with passkey treated as a smoke test inside the broader battery rather than the sole proxy for success.

**status (A-C)**: pending implementation, cpu-battery integration, and prosecutor pass. none of the three interventions is currently coded as a preset in `god_machine.py`.

**status (D, E)**: not yet scoped to the new battery. selection depends on outcome of any A-C cpu gate and on whether the failure localises to write formation, read formation, or output usage.

## curriculum track

the teaching PDF curriculum remains preserved as a review-ready package. the master specification lives in the plan file at `~/.claude/plans/compressed-dancing-haven.md`. this section gives the one-line-per-chapter outline and the production protocol summary for curriculum work; it does not override the 2026-04-26 neural-model master-plan research phase.

![curriculum scale summary](assets/statistics/s2_curriculum_scale_summary.svg)

### 36-chapter outline

#### phase 1 — foundations of math (ch. 1-8)

- ch. 1 — what a number means (arithmetic, variables, functions, injective/surjective/bijective)
- ch. 2 — how things change (derivatives from first principles, chain rule, partials)
- ch. 3 — how things accumulate (integrals, fundamental theorem of calculus, summation)
- ch. 4 — the language of direction (vectors, dot products, norms, projections, orthogonality)
- ch. 5 — the language of transformations (matrices as linear operators, outer vs inner product, rank, nullspace)
- ch. 6 — the language of multi-dimensional change (Jacobian, gradient, Hessian, Taylor expansion)
- ch. 7 — the language of uncertainty (probability, distributions, expectation, variance, Bayes)
- ch. 8 — the language of information (entropy, KL, mutual information, channel capacity, rate-distortion)

#### phase 2 — foundations of biology (ch. 9-14)

- ch. 9 — what a cell is (membrane, organelles, DNA, transcription, translation)
- ch. 10 — electricity in a cell (ions, Nernst equation, resting potential, Goldman equation)
- ch. 11 — the action potential (Hodgkin-Huxley, sodium and potassium currents, refractory period, propagation)
- ch. 12 — the synapse (chemical vs electrical, neurotransmitters, receptors, short-term plasticity)
- ch. 13 — the neuron as a compartmental computer (dendrites, soma, axon, spike initiation, BAC firing)
- ch. 14 — circuits of neurons (excitation-inhibition balance, cortical microcircuit, PV/SST/VIP interneurons)

#### phase 3 — computation in the brain (ch. 15-20)

- ch. 15 — what a neuron computes (linear integration, nonlinear activation, firing rate vs spike timing, tuning curves)
- ch. 16 — what a population represents (rate, temporal, sparse, population coding; Fisher information)
- ch. 17 — how the brain learns (Hebbian, STDP, BCM, three-factor learning, neuromodulation as third factor)
- ch. 18 — what the cortex does (hierarchical feature extraction, predictive coding, free-energy principle)
- ch. 19 — what the hippocampus does (pattern separation in DG, pattern completion in CA3, indexing theory)
- ch. 20 — what consolidation does (sleep stages, sharp-wave ripples, hippocampus-to-cortex transfer, reconsolidation)

#### phase 4 — math for machine learning (ch. 21-26)

- ch. 21 — the loss function (MSE, MAE, cross-entropy, geometric interpretation of minimization)
- ch. 22 — gradient descent (algorithm, learning rate, convergence, stochastic vs batch, momentum, Adam)
- ch. 23 — backpropagation (chain rule through computational graphs, reverse-mode autodiff, vanishing/exploding gradients)
- ch. 24 — neural networks (MLP, activations, universal approximation, residual connections)
- ch. 25 — recurrence and state-space dynamics (RNN, LSTM, GRU, state-space model formulation, gated layers as ODE discretizations)
- ch. 26 — attention as an operation (scaled dot-product, softmax, transformer, linear attention, rotary positional encoding)

#### phase 5 — advanced architectures and compression (ch. 27-32)

- ch. 27 — associative memory (Hopfield networks, Hebbian storage, pattern completion, capacity ~0.14N)
- ch. 28 — modern Hopfield networks (Ramsauer et al. 2020, exponential capacity via softmax, connection to attention)
- ch. 29 — fast-weight memory (outer-product state, delta rule, Kohonen, Schlag, Titans test-time memory)
- ch. 30 — sparse and quantized coding (k-WTA, ternary encoding, Bitnet b1.58, VQ-VAE, residual VQ, rate-distortion geometry)
- ch. 31 — generative and indexed memory (NeRF, memorizing transformers, Larimar, world models)
- ch. 32 — compression beyond quantization (project's six-mechanism thesis: predictive filtering, generative replacement, schema-delta, content-addressable indexing, manifold abstraction, reconsolidation)

#### phase 6 — paper implementation (ch. 33-36)

- ch. 33 — how to read a paper (systematic method, five-pass reading, extracting load-bearing equations)
- ch. 34 — implementing modern Hopfield networks from Ramsauer 2020 (paper-to-code walkthrough, working PyTorch)
- ch. 35 — implementing Titans from the paper (fast-weight MLP with surprise-gated updates, passkey benchmark)
- ch. 36 — implementing a slot-addressed memory (the project's substrate as the final worked example)

### production protocol summary

per chapter: (1) outline draft → (2) user approves outline → (3) parallel research agents across wiki + cited papers + classical textbooks + online resources + reference implementations + recent papers → (4) prosecutor agents validate research for factual accuracy, internal consistency, completeness, naming compliance, and pedagogical coverage → (5) 20-25 page LaTeX draft → (6) prosecutor agents validate draft → (7) user reviews draft → (8) revision → (9) PDF finalization → (10) next chapter. full protocol in the plan file.

### naming rule for the curriculum

published-technique names ("MLA", "KDA", "Hopfield", "Hebbian", "STDP", "BCM", "k-WTA", "ternary spike") are permitted in chapters ONLY when (a) quoting an external source or (b) naming an external paper's architecture (e.g., "in Ramsauer 2020, the architecture is called 'modern Hopfield networks'"). the project's own components are always named by what they do mathematically or by the project glossary terms (`matrix memory`, `compressed attention`, `slot memory`, `output gate`, `surprise ratio`, etc.). every chapter's research output and draft passes a naming-compliance prosecutor before finalization.

### decision gate for the curriculum

the curriculum's success criterion is defined in the plan file: at the end of phase 6 (after ch. 36 finalizes), the reader can read a raw research paper, extract the mathematical operation, translate it to working code, and evaluate it. if at that point the reader cannot complete the three paper-implementation chapters independently (ch. 34, 35, 36), the curriculum has failed and must be revised before declaring completion. paid compute remains paused through the curriculum support lane and the neural-model research phase; it can return only after broad lane research, mechanism dossiers, dossier-driven test material, oracle compression analysis where relevant, a tiny trainable mirror, cpu controls, telemetry, prosecutor-clean docs, and one explicitly selected hypothesis exist.

## experimental method

we have two testing environments in this project:

1. **simulation directory** at `neuroloc/simulations/` — pure-python and brian2 scripts that test biological and mathematical hypotheses on cpu in seconds to minutes. used to validate candidate ideas before paid compute.
2. **paid compute** at h200 scale via runpod — used to validate candidates that survive simulation.

the experimental method for the neural-model backlog has a pre-intervention preparation stage:

0. **prepare broad research and test material before implementation.** use the six-lane master plan, `wiki/synthesis/neural_model_paper_spine.md`, `wiki/synthesis/oracle_compression_analysis_plan.md`, `wiki/synthesis/neural_model_research_test_material_plan.md`, and the mechanism dossiers as the required front door. no test is accepted until its dossier states the claim, test material, control set, telemetry, and kill condition. the hard symbolic test-material package provides the completed symbolic-verification layer. the 2026-04-27 eligibility-gated local-commit dossier, symbolic contract, generator, deterministic evaluator, and suite are now implemented and documented. the first oracle compression analysis and frontier split are also implemented and documented. the tiny trainable neural-model mirror follows only after a narrow family-specific learned-codec target is justified.

0a. **novelty side-tracks require proof packaging.** building the neural model may expose multiple potentially paper-worthy mechanisms. a candidate novelty is not adopted as project-native until it has a compact proof package: mathematical operation, why prior art does not already cover the claim, oracle or symbolic bound, trainable test plan, controls, telemetry, kill condition, and a statement of what would make it useful. side-track papers are allowed, but they inherit the same no-paid-compute gate unless explicitly reauthorised after cpu evidence.

after that preparation stage, when the architecture backlog is explicitly reactivated:

1. **pick exactly one intervention** from the A-E list in `wiki/synthesis/substrate_requires_architectural_change.md`. A (output gate init), B (auxiliary retrieval loss), and C (orthogonal key init) remain the cheapest single-file changes. D (warm-start curriculum) and E (substrate replacement) remain the heavier branches.
2. **map the candidate to the phase-1 battery before writing code.** the candidate must name which gate or gates it is expected to affect: recognition / belief-state formation, associative recollection, interference resistance, delayed use under partial observability, episodic reuse, or iterative reasoning / counterfactual rollout. if the claim does not map to at least one explicit gate and one explicit control, it is not acceptable for implementation.
3. **build or extend the cpu simulations first.** the minimum local surface is the one defined in `wiki/synthesis/phase1_evaluation_surface_for_neural_models.md`. as of 2026-04-22 that surface already includes clean associative recall, key-interference sweeps, delayed-use worlds, episodic separation / completion / novelty, episodic replay / reuse, and context-gated routing. the main remaining simulation gap is the latent-world deliberation / iterative-rollout probe. phase 2 is not a separate stack after this; when the bridge is needed, use the synthetic shared-world method in `wiki/synthesis/synthetic_shared_world_bridge.md` so every modality remains a view of one exact latent state.
4. **wire trainability controls into every candidate.** oracle-write / learned-read, learned-write / oracle-read, hand-placed addresses or hand-opened gates, gate-init sweeps, key-orthogonality or slot-usage-entropy sweeps, and matched no-memory / recency-only / shuffled-address baselines are part of the method, not optional extras.
5. **run prosecutor on simulation code before execution, then write the evidence article.** every new or modified simulation gets prosecutor review before running, a `wiki/tests/<artifact>.md` or synthesis article after running, and prosecutor review on the resulting wiki text. zero-findings rule applies at both stages.
6. **only after the cpu battery passes may the candidate touch the paid model path.** for A, B, or C this means implementing behind an explicit preset flag in `neuroloc/model/god_machine.py`, adding preset-specific tests, and wiring the matching NM evaluation metrics so the model is not judged by LM loss plus passkey alone. for D or E this means the new substrate's own cpu battery first.
7. **smoke test the selected preset before any paid launch** and refuse to launch until both the prosecutor pass and the cpu battery are clean.
8. **run a short h200 benchmark** on the preset at full launch shape (`NM_MAX_STEPS=20`, `NM_SKIP_VALIDATION=1`, `NM_SKIP_EVAL=1`). the existing benchmark-manifest guard in `god_machine.py` still applies.
9. **launch the full paid run only after the broader eval surface exists.** for phase-1 interventions the controlled corpus remains the synthetic cognition corpus unless the candidate specifically belongs to the shared-world bridge. no paid run is acceptable if the only persisted gate is passkey.
10. **hard gate**: passkey @ 256 ≥ 1/100 with Wilson 95% lower bound above zero remains necessary, but it is no longer sufficient. the same run must also report at least one second phase-1 metric aligned with the candidate's claim, and that metric must beat matched controls. inconclusive eval fails closed.
11. **if the gate is cleared**: the intervention unblocked SGD for retrieval on more than one axis. next step is to characterize the trained substrate across the rest of the battery and only then test feature combinations.
12. **if the gate is NOT cleared**: record which stage failed - recognition, write formation, read formation, output usage, interference resistance, delayed use, or iterative refinement - update `substrate_requires_architectural_change.md` or the relevant synthesis article, and pick the next intervention from the ranked list. do not retry the same candidate with cosmetic hyperparameter churn.
13. **analyse every completed run** and update this file, the run cards, and the relevant synthesis notes before the next launch.

each phase has a wiki article deliverable that captures what we learned. each phase has a prosecutor pass on its deliverables. no phase is complete until prosecutor returns clean.

### compression research track (parallel with run sequence)

compression is a primary research direction for the project, not a secondary optimization. the six biological mechanisms compound to 10^10-10^12x effective compression in biological memory; the project's matrix memory currently implements zero of the six.

current compression-track state:
1. **correction-field result**: both the synthetic and trained-prediction cpu simulations returned `memory_capacity_gain = 0`. prediction residuals improve reconstruction quality, but they do not increase the matrix memory's own retrievable pattern count. for the current matrix memory, correction-field is therefore a reconstruction-side additive bias, not a memory-side compression win.
2. **current implication**: do not treat correction-field as the next value-compression run candidate for the matrix memory. the matrix memory's capacity ceiling appears structural with respect to what is stored.
3. **what remains useful from that line**: predictive filtering, surprise-gated writing, and residual reconstruction remain meaningful ideas, but they need either a different substrate or a different success criterion than "more patterns stored in the same matrix."
4. **current compact-memory proof surface**: `wiki/synthesis/indexed_reconstruction_compression.md` is the current math target after the correction-field null result. the candidate object stores compact addresses, schema or latent ids, residual codes, and provenance, then reconstructs through a shared decoder and replay rewrite under an explicit rate-distortion objective. this is a proof obligation, not a validated architecture.
5. **recent spiking-source implication**: the 2026 complemented-neuron ternary-snn paper and neuronspark-0.9b both point toward preserving membrane or subthreshold state around discrete events. they support polarity/history-preserving local state and membrane-summary channels as future cpu-probe ideas, but they do not prove a retrieval-capacity gain for the current model.
6. **backlog stance during the master-plan phase**: keep compression work cpu-first and subordinate to the broader six-lane neural-model plan. the current architecture-translation surface for the long-range tiered direction is `wiki/bridge/memory_compression_to_tiered_architecture.md`, but it is a phase-6+ sketch, not an immediate execution item.

## decision rules

- **every new preset must explicitly set `alpha_log_mean`.** inherited defaults are a bug class. see `wiki/mistakes/run2_slot_memory_decay_copy_paste.md` for why — run 2 wasted paid compute because the slot preset inherited the broken `alpha_log_mean=-0.5` default that causes state evaporation to `10^-109` over 256 tokens, a failure mode already documented in `wiki/synthesis/linear_attention_retrieval_wall.md` before the run launched.
- **`_assert_preset_retention_safe` is the structural enforcement of the rule above.** the function runs at every `_resolve_preset` call and raises if a preset uses DELTA or SLOT layers without overriding `alpha_log_mean`. process rules failed to prevent run 2 because "remember to check" is not a mechanism. the code-level assertion is. any future preset that silently inherits the broken default will fail at config resolution, before smoke test, before paid compute.
- **closed 2026-04-17: god_machine.py startup warns and fails when any layer's `use_fla=False` after a preset asks for FLA.** the FLA-silent-fall-through episode at the second `run2_slot_memory_retention_fixed` launch lost 17 minutes of paid h200 because the runtime had no `flash-linear-attention` package installed and every SlotMemory layer fell through to the python recurrent loop with no warning anywhere in the launch logs. fix in commit `edcfe5d` pinned the package in `requirements.txt`. the structural startup-time guard `_assert_fla_available_if_requested` in `god_machine.py` (committed 2026-04-17 as part of the run3_cognition_phase1 launch prep, see the update-history entry for that date) raises at preset resolution whenever a preset sets `use_fla_if_available=True` but the runtime cannot import `flash_linear_attention`. `NM_FORCE_NO_FLA=1` is the escape hatch for CPU smoke tests and intentional non-FLA runs. this bug class is now structurally closed, not process-enforced. mistake doc: `wiki/mistakes/run2_slot_memory_fla_silent_fall_through.md`.
- **every prosecutor pass on a new preset must explicitly verify each init value against `linear_attention_retrieval_wall.md`.** the prosecutor prompt must ask: "what init values does this preset rely on by default? are any of them documented as failure modes in `mistakes/` or `synthesis/`?" this is a hard rule.
- **the "four stacked failure modes" theory from `linear_attention_retrieval_wall.md` is now superseded.** the first four paid runs (god_run, god_run_v2, run1_baseline_noerasure, run2_slot_memory) all had `alpha_log_mean=-0.5` active via inherited Config defaults, so state evaporation was present in every prior failure and the role of the other three theorised modes (O(d) capacity, correlated keys, noise contamination) could not be isolated. the fifth paid run (`run2_slot_memory_retention_fixed`, 2026-04-15) explicitly fixed retention to `alpha_log_mean=5.0` (alpha_eff=0.9933, retention 0.18 at 256) and trained cleanly to val_bpb 1.4777. partial eval still produced passkey 0/100 at 256 and 1024. that result rules out state evaporation as the sole cause of the prior failures and reframes the diagnosis: the language-modelling training objective on fineweb-edu does not exercise the memory substrate at all (mean output gate stayed at 0.018 throughout the run, never opened). see `wiki/synthesis/training_objective_vs_architectural_goal.md` for the full analysis.
- **run 2 conclusion (updated after the fifth paid run, 2026-04-15; superseded by run3 below)**: the slot substrate had then been tested under non-evaporating retention and still produced 0/100 passkey on natural-text training. cpu gates A and B verified it retrieved in isolation, so the next discriminant was a corpus that required memory routing. that discriminant was later executed as run3 and returned 0%, so corpus change alone is no longer an active launch path.
- **updated after run3, 2026-04-17**: the corpus-change hypothesis was executed as `run3_cognition_phase1` on a synthetic cognition corpus where retrieval is 50% of training blocks. the substrate still produced 0/100 passkey at 256 and 1024, and val_bpb plateaued at the alphabet prior from step 150. the corpus is not the obstacle either. current guidance supersedes the prior rule: do not authorise a paid run on the current slot substrate with only corpus or retention or other hyperparameter changes. any future paid-resume path must carry one of the ranked architectural interventions from `wiki/synthesis/substrate_requires_architectural_change.md` (A: open output gate init; B: auxiliary retrieval loss; C: orthogonal key init; D: warm-start curriculum; E: substrate replacement). A-C were identified as low-edit candidates, but the 2026-04-23 plan now requires dossier-driven test material and one-mechanism cpu gates before any paid work.
- **updated 2026-04-21**: passkey @ 256 is now a smoke test inside a broader phase-1 neural-model battery, not the sole backlog gate. when the architecture track resumes after the curriculum, interventions must first clear the cpu battery defined in `wiki/synthesis/phase1_evaluation_surface_for_neural_models.md` with trainability controls, and the phase-2 extension path is the synthetic shared world in `wiki/synthesis/synthetic_shared_world_bridge.md`. a paid run is not acceptable if the model still only knows how to report LM loss plus passkey.
- **if a paid run returns 0% passkey, the first question is always**: did this run reproduce a known failure mode from `mistakes/`? if yes, the learning is zero; the mistake cost pod time. do NOT launch a follow-up run that repeats any same inherited-default failure.
- **if the smoke test on pod fails before training launches**: halt. do not launch training. fix locally first.
- **if a supporting simulation contradicts a completed run artifact**: treat the run artifact as the launch gate, document the contradiction, and use the simulation only to target the next low-cost hypothesis test.

## prior runs index

- **`god_run`**: 2026-04-11. 283m params. five-feature bundle (20% rate-coded compression on keys and values, delta erasure, activity-adaptive decay, compartment-split feedforward block, compressed attention) plus always-on imagination probe and predictive-coding diagnostic head. trained successfully (final val 1.3950 bpb). long-distance recall: 0/20 at every tested distance. marker-gated copy: 0/20 at every tested length. probe metric stream lost approximately 35 keys due to a step-logger bug. run card at `neuroloc/output/god_run/run_card.md`.
- **`god_run_v2`**: 2026-04-12. same architecture as `god_run`, with all 17 prosecutor findings F1-F17 plus 14 follow-up findings G1-G14 fixed before launch. critical fix: F1 corrected the recurrent path's effective decay coefficient from `sigmoid(logsigmoid(pre))` to `exp(logsigmoid(pre))`, eliminating a ~28% train/eval divergence. probe metric stream now records 74 keys per step. trained (final val 1.4453 bpb, ~0.05 worse than v1). long-distance recall: 0/100 at distances 256, 1024, 4096 (Wilson 95% upper bound 3.7%). marker-gated copy: 0/100 at lengths 256, 512, 1024, 2048. perplexity at length series, all five points: 1.9254 at 256 tokens, 1.8941 at 512, 1.5373 at 1024, 1.4776 at 2048, 1.4192 at 4096 (monotonic decrease, confirming the attention path uses context). delta state structure probe: structure_ratio 0.977, pairwise cosine 0.003 (near-orthogonal, statistically indistinguishable from random state of equal Frobenius norm). **the F1 math fix did not recover recall.** run card at `neuroloc/output/god_run_v2/run_card.md`.
- **`run1_baseline_noerasure`**: 2026-04-14. 353m params. matrix memory, dense k/v, no erasure, all auxiliary features off. final val_bpb 1.4499. passkey 0/100 at 256, copy 0/100 at every length. same result as god_run_v2, confirming the matrix memory substrate cannot retrieve at 256 tokens. decay inherited alpha_log_mean=-0.5 (state evaporation, see linear_attention_retrieval_wall.md evidence 4). run artifacts at `neuroloc/output/run1_baseline_noerasure/`.
- **`run2_slot_memory`**: 2026-04-15. 355m params. slot memory substrate with softmax addressing over 64 learned prototype keys per head, FLA-accelerated via `fused_recurrent_simple_gla`. final val_bpb 1.5107. passkey 0/100 at 256/1024/4096, copy 0/100 at every length. delta_state_structure_probe returned `{"error": "no DELTA layers had populated state"}` because the preset replaces every DELTA block with SLOT and the probe only reads DELTA blocks — no structural state evidence is available from the artifact. the preset inherited `alpha_log_mean=-0.5` from Config defaults, reproducing the EXACT state-evaporation bug documented in `wiki/synthesis/linear_attention_retrieval_wall.md`. the slot-substrate hypothesis is NOT tested by this run; the run measured the inherited-decay-bug, not the substrate. mistake documented in `wiki/mistakes/run2_slot_memory_decay_copy_paste.md`. fix committed as `7abb781` (alpha_log_mean=5.0).
- **`run2_slot_memory_retention_fixed`**: 2026-04-15. 355m params. slot memory substrate as above but with `alpha_log_mean=5.0` (alpha_eff=0.9933, retention 0.18 at 256 tokens) explicitly set in the preset, FLA-accelerated via `fused_recurrent_simple_gla` actually present this time (commit `edcfe5d` pinned `flash-linear-attention` after a previous launch attempt fell through to the python recurrent loop and was killed at 17 min). 4000 steps on fineweb-edu, 131,072,000 tokens, 72 min wall-clock at 33,000 tok/s. final val_bpb 1.4777 (best, vs god_run_v2 1.4453). eval suite ran partially: passkey@256 = 0/100 (Wilson 95% CI upper 3.7%), passkey@1024 = 0/100 (same CI). user halted pod before passkey@4096, selective_copy, perplexity-at-length, or delta_state_structure_probe completed. retrieval_gate not persisted because eval did not finish. interpretation: this is the FIFTH paid run with 0% passkey at 256, the FIRST with retention not in the evaporating regime, and the slot substrate verified active. the consistent 0% across substrates and retention regimes now points the diagnosis at the training objective rather than the architecture (see `wiki/synthesis/training_objective_vs_architectural_goal.md`). run card at `neuroloc/wiki/tests/run2_slot_memory_retention_fixed_results.md`.
- **`run3_cognition_phase1`**: 2026-04-17. 355m params. slot memory substrate (24 SLOT + 4 ATTN), alpha_log_mean=5.0, FLA-accelerated via `fused_recurrent_simple_gla` (guard `_assert_fla_available_if_requested` enforced the FLA availability pre-flight). trained on synthetic cognition corpus from `neuroloc/data/cognition_corpus.py` (200m bytes total, mix 50% passkey / 30% kv-recall / 20% copy, passkey distances {16, 64, 128, 256, 512}, hex ASCII alphabet). 4000 steps, 131,072,000 tokens, 4322 sec wall-clock = 72 min at steady 32,800 tok/s. final val_bpb 6.3519 (best, recorded at step 2150; never descended past it). val_bpb plateaued at the alphabet prior from step 150 and stayed there for 3850 more steps. partial eval before user stopped pod: passkey@256 = 0/100, passkey@1024 = 0/100. retrieval_gate not persisted. interpretation: this run was the predicted discriminant from `wiki/synthesis/training_objective_vs_architectural_goal.md` between "LM loss is the problem" and "the architecture cannot be trained"; the architecture-cannot-be-trained branch fires. on a corpus that explicitly requires memory routing in the loss, the slot substrate still does not learn retrieval within 4000 steps at 355m params. the prior diagnosis is confirmed in its structure but worst case in its outcome. next step is architectural intervention — see `wiki/synthesis/substrate_requires_architectural_change.md`. run card at `wiki/tests/run3_cognition_phase1_results.md`.
- **prior runs `run_001` through `run_011`**: see `state/program_status.yaml` and `docs/EXPERIMENT_LOG.md`. earlier low-scale retrieval probes did exist, including passkey in `run_003` and later copy/passkey checks in the small-scale phase-2 branch, but they are not the authoritative gate for the current neural-machine baseline. the dedicated paid run surface still records longer-distance recall and marker-gated copy, but as of the 2026-04-21 methodology update passkey at 256 is treated as a smoke test inside a broader phase-1 battery rather than the sole blocking gate.

## open research questions

1. does the architecture's asymmetric outer-product matrix memory support content-addressable retrieval at any encoding?
2. if yes, which encoding family preserves the most retrievable structure?
3. does the optimal encoding for the key half differ from the optimal encoding for the value half? biological cortex pattern-separates keys aggressively (~5% sparse) and stores values densely. does the project's architecture want the same asymmetry?
4. after the standalone overwrite sweep at `decay=0.90` showed that erasure hurts every tested encoding at the first useful 32-pattern knee, does training ever create a regime where overwrite becomes net helpful once keys remain dense?
5. the standalone decay sweep now shows that retention matters sharply: exact-query recall at `d_head=64` first reopens at 32 patterns around `decay=0.90` and at 64 patterns around `decay=0.95`. can the trained model sustain a similarly slow retention regime without collapsing optimization or saturating the state?
6. is there a structural change to the architecture that would allow a different memory operation to handle retrieval, while the matrix memory handles a different role (e.g., short-term smoothing)?
7. once we have a validated encoding from simulation, does it scale to the h200 training environment without losing its property — i.e., does standard gradient descent at 283m params find the encoding's predicted operating point?
8. after the correction-field simulations returned `memory_capacity_gain = 0` under both synthetic and trained predictions, where can predictive filtering still help: write policy, reconstruction quality, or a different substrate entirely?
9. if reduced write frequency alone does not raise matrix-memory capacity, which substrate families can actually convert lower write load into more retrievable content?
10. does surprise-gated writing reduce write frequency without hurting recall on the tokens that matter when evaluated on the broader phase-1 neural-model battery rather than on passkey alone?
11. can a multi-tier compression scheme add semantic compression above the matrix-memory ceiling without violating the fixed-size continual-learning constraint?
12. what benchmark surface should replace verbatim-only retrieval tests when the candidate compression mechanism reconstructs semantically faithful but non-verbatim content?

## glossary

this is the project's own vocabulary. every term here describes a thing the project is building, not a thing imported from a published paper. each entry maps a project term to its mathematical definition. adding a new term to the glossary is part of the prosecutor cycle.

- **architecture**: the neural model the project is building. it has no external name and is not currently fixed to the old matrix-memory plus compressed-attention layer stack. as of 2026-05-09, the active local architecture target is an actual 100k-parameter compression-first model with learned local state, context, working memory, episodic memory, long-term memory, write/read dynamics, replay or rewrite, latent rollout or recombination, action and language answer decoding, useful-state-density/compression boundary, training objective, controls, telemetry, and kill conditions.
- **matrix memory**: the per-layer state-keeping operation `S_t = a * S_{t-1} + b * k_t * v_t^T`, optionally with erasure `S_t -= a * b * k_t * (k_t^T * S_{t-1})`. shape `(batch, num_heads, head_dim, head_dim)`. read via `o_t = q_t^T * S_t`. exists in 24 of the 28 layers.
- **compressed attention**: the per-layer operation that projects the residual stream to a low-rank cached representation per token, then computes scaled dot-product attention over the cache with rotary positional encoding concatenated onto the keys. exists in 4 of the 28 layers.
- **decay coefficient `a`**: the per-step retention factor in the matrix memory's update. computed at the start of each forward pass as `exp(logsigmoid(alpha_log_base))` for the static case, or with an optional adaptive subtraction term for the activity-dependent case. range (0, 1).
- **write gate `b`**: the per-step write strength in the matrix memory's update. computed per token as `sigmoid(beta_proj(x_t))`. range (0, 1).
- **erasure**: the optional subtraction term `a * b * k_t * (k_t^T * S_{t-1})` in the matrix memory's update. when present, every write subtracts the projection of the existing state onto the incoming key direction before adding the new key-value outer product, so writes targeting the same direction overwrite cleanly instead of stacking. when absent, the matrix memory is a pure accumulator: every write adds, nothing subtracts, and writes targeting the same direction stack indefinitely until decay removes them. the subtraction is applied in both execution paths when the toggle is true; which path runs depends on backend availability and sequence shape.
- **input encoding**: the function applied to keys, values, and queries before they enter the matrix memory's update. it remains a tunable part of the mechanism, but the current paid-run branch is not an encoding-only sweep because the earlier simulation work already closed that branch negative at the old operating point. the candidate space currently includes: identity (no transformation), three-level magnitude quantization with one global threshold per layer, three-level magnitude quantization with one threshold per dimension, fixed-fraction absolute-magnitude selection (keep the largest k of d entries, zero the rest), sign-only encoding (map to plus-or-minus one based on sign of the input, zero only when input is exactly zero), continuous bounded encoding (saturating nonlinearity with no zeros), and any new encoding that simulation work justifies adding.
- **long-distance recall task**: the evaluation in which a five-byte sequence is hidden inside a longer noise sequence and the architecture is asked to reproduce it after the noise. the architecture passes if it produces the correct sequence. tested at distances 256, 1024, 4096 tokens. for the current run-1 branch, passkey at 256 tokens is the hard gate; the longer distances are secondary diagnostics until the baseline clears that first gate.
- **marker-gated copy task**: the evaluation in which the architecture is asked to copy a marker-prefixed substring from earlier in the input. tested at lengths 256, 512, 1024, 2048 tokens. used as a secondary retrieval diagnostic for the current run-1 branch.
- **structure ratio**: the metric `mean ||probe_output|| / mean ||random_state_output||` measured over 32 random query vectors against the matrix memory's accumulated state after a fixed warmup. structure ratio near 1.0 means the matrix memory's state is statistically indistinguishable from random noise of the same Frobenius norm; structure ratio significantly below 1.0 (or above 1.0 with high pairwise cosine) means the state has stored content. used to diagnose whether the matrix memory has accumulated meaningful structure during training.
- **firing rate**: the fraction of dimensions in an encoded vector that are nonzero after the encoding is applied. depends on the chosen encoding and on the input distribution. measured per layer per step.
- **correction field**: the matrix memory operating mode in which values are projected from prediction residuals instead of raw hidden states. the state S becomes a map from query directions to prediction corrections: for any query q, `q^T @ S` returns the expected correction to the model's own prediction of the queried content. retrieval reconstructs as `prediction + correction`. described in `wiki/synthesis/correction_field_memory.md`.
- **prediction residual**: the vector `r_t = h_t - f(h_{t-1})` where h_t is the actual hidden state at timestep t and f is a lightweight learned prediction function. the residual is what the prediction function failed to anticipate. in the correction-field design, the value projection W_v operates on r_t instead of h_t.
- **surprise ratio**: the scalar `s_t = ||r_t||^2 / (||h_t||^2 + eps)` measuring how much of the hidden state's energy was unpredicted. range [0, 1]. s_t near 0 means the token was fully predictable; s_t near 1 means it was fully novel. used to modulate the write gate in the correction-field design.
- **self-compression**: the property of the correction-field design whereby the state's information content decreases as the model's prediction function improves during training. the compression ratio is not a hyperparameter but an emergent consequence of learning quality. early in training (bad prediction), the state stores approximately raw values; late in training (good prediction), the state stores only small corrections.
- **depth gradient**: the predicted phenomenon in the correction-field design where deeper layers' states carry less total information than shallower layers' states at convergence, because deeper layers' prediction heads explain more of the hidden-state variance. if confirmed, this creates a naturally emergent compression hierarchy within the existing 24-layer stack.
- **slot memory**: the proposed replacement substrate for the matrix memory. a per-head buffer of shape `(B, H, N, D)` where N is the slot count (default 64) and D is the per-slot content dimension (default 64). unlike the matrix memory, slots do not decay — they overwrite. reads use softmax addressing: `o = softmax(q @ slot_keys.transpose(-2,-1)) @ slot_values`. softmax restores exponential capacity per ramsauer et al. 2021, escaping the matrix memory's `O(d)` linear ceiling.
- **softmax addressing**: the read mechanism of the slot memory. given a query q and slot keys K of shape (N, D), the attention weights `softmax(q @ K^T / sqrt(D))` are used to produce a weighted sum of slot values. the softmax's exponential suppression isolates a single matching slot even under non-orthogonal keys, unlike linear `q^T S` readout which averages across all stored patterns with interference.
- **slot write policy**: the rule that decides which slot receives each new write. three options: (A) surprise-gated with least-recently-used allocation (uses the correction-field mechanism's prediction-residual surprise signal), (B) learned allocation scoring (a softmax over learned slot-key embeddings), (C) fixed hash allocation (baseline). option A is the default starting point.
- **output gate**: a sigmoid-bounded scalar per head that multiplies the slot memory's output before it is added to the residual stream. initialized near zero (via `gate_init = -4.0` so `sigmoid(-4) ≈ 0.018`). during training, the model learns to open the gate only for layers/heads where the slot memory's output actually helps. prevents noise injection from under-trained heads.
- **slot age / recency counter**: a per-slot counter that tracks how long since each slot was last read or written. used by the least-recently-used write policy to select which slot to overwrite when a new write arrives.
- **prosecutor pass**: a code review carried out by the `feature-dev:code-reviewer` agent (the non-claude-suffix variant). the review identifies findings ranked by priority. the rule is that every finding must be either fixed or explicitly waived in writing by the user before the related code is allowed to run on paid compute or to be published as a wiki article.

## update history

every entry below is an append-only record of a change to this file. format: `date — author — one-line description of what changed and why`. the project's sole author is the human who runs the project; no other authorship credit is given here.

- **2026-04-12** — deyan todorov — file created. captures god_run_v2 result, the bundle-is-broken diagnosis, the immediate next action of running three existing simulations followed by writing a new one, and the project glossary's first entries. supersedes the implicit "what happens next" knowledge that previously lived only in conversation context.
- **2026-04-12** — deyan todorov — phase A simulation round complete. three existing simulations (`neuroloc/simulations/memory/capacity_scaling.py`, `neuroloc/simulations/prototypes/rate_coded_spike.py`, `neuroloc/simulations/sparse_coding/hierarchical_ternary.py`) ran on cpu, producing metrics that consistently rank sign-only encoding above three-level magnitude quantization on symmetric outer-product memory recall. continuous bounded encoding produces high cosine similarity but zero exact recall. zeros in the encoded vector destroy the address space. the gap to the project's architecture (asymmetric, single-step, decayed, optionally-erased) is identified and documented in `neuroloc/wiki/tests/encoding_simulation_round_a.md`. round B (asymmetric simulation) is the next deliverable.
- **2026-04-12** — deyan todorov — phase B simulation round complete on partial pattern-count sweep [8, 16, 32, 48]. the new simulation `neuroloc/simulations/memory/asymmetric_outer_product_recall.py` implements the project's actual matrix memory operation (asymmetric, single-step, decayed, optional erasure) at head dimension 64 and tests seven candidate input encoding configurations. 24 trials per cell, 12,096 trial records. **leading finding: at head dim 64, no encoding tested produces meaningful recall above 8 patterns. the only crossing of 0.5 mean cosine recall is dense-key plus sparse-value at 8 patterns (0.51).** at 32 patterns the leader is tanh-bounded continuous encoding at cosine 0.19 and top-1 accuracy 0.21 (random chance is 0.031). the encoding leader reversed from round A: round A's sign-only winner is now the second-best at low loads and degrades as fast as the others above 16 patterns. the result is a capacity-ceiling finding, not an encoding finding. at the time, the rank-ceiling test (pattern_count = 64 = head_dim) was deferred because a local windows mingw-w64 numpy build segfaulted at that pattern count. four candidate next moves are listed in `neuroloc/wiki/tests/encoding_simulation_round_b.md`: enlarge head dim, route long-range through compressed attention, test the erasure-on cells, replace the matrix memory entirely. user direction was needed before any paid-compute code changes.
- **2026-04-12** — deyan todorov — head-dimension sweep complete for the asymmetric matrix-memory simulation at head dims 32, 64, 128, 192, and 256. the threshold curve is sub-linear: $p^*(d) = {16, 16, 16, 24, 32}$. every threshold-crossing cell comes from the slower-retention setting `decay=0.8`; at `decay=0.4`, only the 32-dimensional run crosses 0.5 mean raw cosine, and only at 8 patterns. this partially answers candidate move (a): wider state helps, but it is not a standalone fix at the current faster-forgetting operating point. the next priority is retention and overwrite analysis, not a pure head-dimension paid run.
- **2026-04-12** — deyan todorov — canonical plan updated to the post-`god_run_v2` dense-baseline branch. the next scheduled paid run is run 1 baseline dense on h200 with dense keys, dense values, and erasure on; the simulation track stays focused on retention and overwrite questions; and phase 6a aesthetic logging starts in parallel from the frozen `god_run_v2` artifacts. this entry records the shift away from the older encoding-first launch plan and aligns the plan with `state/program_status.yaml`, `docs/STATUS_BOARD.md`, and the revised sequencing in `neuroloc/spec/blueprint.md`.
- **2026-04-12** — deyan todorov — fine-grained retention and overwrite follow-ups complete at `d_head=64`. the decay sweep shows exact-query recall reopening at 32 patterns around `decay=0.90` and at 64 patterns around `decay=0.95`; the overwrite sweep shows erasure hurts all seven encodings at the first useful 32-pattern knee. the scheduled dense baseline therefore switches from erasure on to `run1_baseline_noerasure`, and a slower static-retention initialization becomes the first contingency if that baseline still fails.
- **2026-04-12** — deyan todorov — correction-field memory design added as the project's novel compression method. the design stores prediction residuals instead of raw values in the matrix memory, compounding predictive filtering, generative reconstruction, and residual coding in one mechanism. five new glossary terms added (correction field, prediction residual, surprise ratio, self-compression, depth gradient). five new open research questions (8-12) added. compression research track added to the experimental method as a parallel track validated by cpu simulation before entering the run sequence. wiki article at `wiki/synthesis/correction_field_memory.md`, simulation prompt at `simulations/prompts/correction_field_capacity.md`. the compression research track is a sub-section within the experimental method that runs in parallel with the main sequential steps 1-11 and has its own three-outcome decision rule.
- **2026-04-14** — deyan todorov — correction-field simulation harness checked in at `simulations/memory/correction_field_capacity.py` and registered in the simulation suite. the harness now separates prediction-only reconstruction, correction-memory-only retrieval, and final reconstruction so capacity and decay-threshold claims can be tied to the memory substrate instead of the synthetic prior. the harness is still pending prosecutor-reviewed run artifacts before it changes any launch decision.
- **2026-04-14** — deyan todorov — correction-field full sweep ran (17568 trials, 471s). headline: memory_capacity_gain = 0 across ALL prediction qualities; reconstruction_effective_capacity = 64 at q>=0.5 vs raw = 32 (gain from prediction, not memory); memory_threshold_shift_by_decay = 0; surprise gating works (write_fraction drops to 1.6% at predictable_frac=0.05, tau=0.99). wiki article rewritten and prosecutor-fixed. artifact: `neuroloc/output/simulation_suites/correction_field_capacity/correction_field_capacity_metrics.json`.
- **2026-04-14** — deyan todorov — trained-prediction correction-field sim complete (`simulations/memory/correction_field_trained_prediction.py`). rank-16 trained heads on correlated synthetic data, rho in {0.5,0.7,0.9,0.99}, d_head {32,64,128}, decay {0.5,0.9,0.95}. **memory_capacity_gain = 0 under trained predictions too.** at d_head=64, decay=0.9, rho=0.99, n=64: raw memory recall = 0.294, residual memory recall = 0.293 (equal within 0.003), prediction-only = 0.369, full reconstruction = 0.492. residual norm ratio only 8% smaller than raw. **the correction-field is a reconstruction-side additive bias, NOT a memory-side compression.** the matrix memory's capacity ceiling is structural and does not respond to what is stored. artifact: `neuroloc/output/simulation_suites/correction_field_trained_prediction_full/`.
- **2026-04-14** — deyan todorov — multi-resolution head split sim complete (`simulations/memory/multi_resolution_head_split.py`). 240 paired trials. **STRONG positive result on long-horizon queries.** at d_head=64 L=512: split (8 fast decay=0.5, 6 medium decay=0.95, 2 slow decay=0.999) achieves rare-class cosine 0.646 vs uniform decay=0.9 at 0.089. split beats uniform on rare and recent classes at every tested stream length, permutation p=0.001. cost: split HURTS immediate-class recall by 0.17-0.31 cosine (fast heads at decay=0.5 attenuate last-4-token content). this cost is mitigable because the compressed-attention layers already carry short-range exact retrieval. recommendation: adopt split in run 3 as a memory-side compression; run 2 can stay as the correction-field (reconstruction-side) or swap to head split. artifact: `neuroloc/output/simulation_runs/memory/multi_resolution_head_split/`.
- **2026-04-14** — deyan todorov — pod benchmark complete. smoke test passed (including alpha_eff math oracle, FLA-vs-recurrent parity, resume correctness, jsonl round-trip, all preset smokes). 20-step fineweb-backed benchmark finished in 1264s total. manifest at `/root/output/run1_baseline_noerasure_benchmark_manifest.json` with preset=run1_baseline_noerasure, status=completed, device=H200 sm_90, config_hash=d911125142824763, all artifact hashes present. **param_count=353707776 (note: 353m, not 283m — the baseline dense config with standard SwiGLU and no multi-compartment has a different parameter count than the full god_run bundle).** training loss decreasing normally (5.74 at step 0 → 2.52 at step 17), mean_fr=0, erasure flag=0 — preset correctly wired. peak gpu_mem=28gb (fine on 144gb H200). **critical throughput finding: 531 tok/sec on the non-FLA recurrent path, step duration ~63s. extrapolating, 4000 steps = ~70 hours of h200 wall-clock, vs god_run_v2's 59 min on FLA (~45000 tok/sec). the non-FLA requirement locks run 1 into a ~85x slowdown versus prior runs. estimated cost at typical h200 rate ($3-5/hr): $210-350 per 4000-step run.** decision required before launch: either accept the 70h non-FLA run, OR update the launch contract to allow FLA for run 1 given the F1 torch.exp fix aligned FLA and recurrent path math. the smoke test's FLA-vs-recurrent parity check passes, so the divergence concern that originally motivated non-FLA is addressed. user call.
- **2026-04-14** — deyan todorov — thinking-loop prototype sim complete (`simulations/reasoning/thinking_loop_prototype.py`). 78k-param tiny model, modular arithmetic task mod 7, ops add/sub/mul, 3 seeds, 15 epochs. findings: K=1→K=2 gives +3.7pp accuracy at N=8 (95% CI [+1.6, +5.8], the only clean positive). K=4+ gains collapse into noise. deeper feedforward baseline beats thinking K=16 by 5-6pp at N=16, N=32 at matched per-step compute. fixed-point reached in 99% of samples by K=32 (contraction, but not discriminative enough). caveats: pilot scale too small, baseline near chance (14-25% vs 14.3% random), 3 seeds only, single task, state-as-extra-token is only one recurrence variant tested. user directive is that thinking is non-negotiable for the first model, so the pilot's job is to inform HOW not WHETHER. cleaner pilot needed before thinking is added to any run. artifact: `neuroloc/output/simulation_runs/reasoning/thinking_loop_prototype/`.
- **2026-04-14** — deyan todorov — **STRUCTURAL FINDING on FLA path**: attempted to flip `use_fla_if_available=True` in run1_baseline_noerasure preset. smoke passed but throughput stayed at ~510 tok/sec. root cause at line 559: `can_use_fla` requires `self.delta_erasure=True`. resolved by adding `_fla_no_erasure_path` using FLA's `chunk_simple_gla` kernel, with full parity validation (four variants: erasure on/off × bcm on/off, max_abs 3.1e-4, rel 1.3e-3 vs recurrent baseline). throughput post-fix: 57,000 tok/sec steady (114x speedup). run1_baseline_noerasure completed in 42 min instead of 70h.
- **2026-04-14** — deyan todorov — **RUN 1 CLOSED. passkey 0/100.** val_bpb 1.4499 (matches god_run_v2's 1.4453 within noise) despite all 5 bundle features removed (no k-wta, no erasure, no bcm, no multi-compartment, no imagination, no pc head). three paid runs converged on the same outcome. the bundle-broken diagnosis from 2026-04-12 is falsified at the substrate level: the base matrix memory itself cannot retrieve.
- **2026-04-14** — deyan todorov — **THEORETICAL DIAGNOSIS COMPLETE.** four independent research sources (external opinion paste + 3 internal research agents) converge: the matrix-memory substrate has four stacked failure modes, any one of which prevents passkey at 256. (1) classical `O(d)` capacity ceiling at ~9 patterns under d_head=64 (ramsauer 2021 proved softmax is structurally required for exponential capacity). (2) learned keys under next-byte loss are correlated not orthogonal (cabannes et al. icml 2024). (3) default retention `alpha_eff=0.377` compounds to `10^-109` over 256 tokens — below float32 epsilon, state physically evaporates. (4) delta output unconditionally added to residual contaminates attn layers' inputs with noise. full synthesis in `wiki/synthesis/linear_attention_retrieval_wall.md` with 11 cited papers.
- **2026-04-14** — deyan todorov — **PIVOT TO SLOT MEMORY.** design committed in `wiki/synthesis/slot_memory_design.md`. replace matrix memory `S ∈ R^{B×H×D×D}` with slot memory `M ∈ R^{B×H×N×D}` where N=64 slots per head. reads use softmax addressing (restores exponential capacity). writes use surprise-gated lru allocation (reuses correction-field mechanism 1 already validated on cpu). explicit output gate initialized near zero (prevents noise contamination during early training). six new glossary terms added: slot memory, softmax addressing, slot write policy, output gate, slot age/recency counter. three cpu simulations specified as paid-compute gate: (A) standalone slot buffer capacity, (B) slot buffer with surprise-gated writes at 256-length filler, (C) integration smoke on tiny byte-level model. no paid compute until all three pass. new pod `ssh root@213.181.111.129 -p 10953 -i ~/.ssh/runpod_parametergolf` is reserved for eventual run 2_slot_memory once cpu gate passes.
- **2026-04-14** — deyan todorov — **CPU experiments A+B complete**, artifact at `neuroloc/output/test_runs/delta_deadweight_experiment.json`. experiment A (1.17m tiny model, 6 delta + 1 attn, 500 steps on 3.8mb synthetic bytes): baseline val_bpb 7.1114, zeroed-delta val_bpb 7.1201, **relative change +0.12%**. **delta-is-dead-weight hypothesis confirmed at tiny scale**: zeroing every matrix-memory output moves val_bpb by a fraction of a percent. the 1 attn layer + mlps do essentially all the useful fit work even without any training incentive to rely on delta. combined with god_run_v2's independent structure probe result (ratio 0.977, pairwise_cos 0.003) at 283m trained scale, this is two-scale convergent evidence that the delta substrate is functionally inert. experiment B (per-head key gram diagonal 2.08 off-diag abs 0.57 ratio 0.275 on all 4 heads; signed-mean ratio 0.046): **learned-key-correlation hypothesis confirmed** under the physically meaningful absolute-ratio statistic. the signed statistic (specified in the original plan) was wrong for this question and reported 0.046, but off-diagonal magnitude as a fraction of diagonal is what measures interference. all four heads exceed the 0.2 threshold in the correct statistic. cabannes et al. icml 2024 mechanism is operating exactly as predicted.
- **2026-04-14/15** — deyan todorov — SlotMemory class implemented in `god_machine.py`. initial version used a Python `for t in range(T)` loop: unusable, 203 tok/sec on H200 (projected 177 hours per 4000-step run). rewrite to use FLA `fused_recurrent_simple_gla` kernel with prototype keys for softmax-addressed soft allocation: 33,000 tok/sec steady, 4000 steps in 72 minutes. **BUT THE PRESET INHERITED `alpha_log_mean=-0.5`**, reproducing the state-evaporation bug this author had documented four days prior in `wiki/synthesis/linear_attention_retrieval_wall.md` evidence line 4. paid run returned val_bpb 1.5107 with 0/100 passkey at every tested length. mistake documented in `wiki/mistakes/run2_slot_memory_decay_copy_paste.md`. fix committed as `7abb781` (alpha_log_mean=5.0, alpha_eff=0.993). pod halted. substrate hypothesis remains untested because the run measured the retention bug, not the substrate. **FOUR paid runs so far, four zero-retrieval outcomes, and the slot substrate has not yet had a single fair test.** new hard rule added to decision rules: every preset must explicitly set alpha_log_mean and every prosecutor pass must cross-check init against `mistakes/` and `synthesis/` articles.
- **2026-04-15** — deyan todorov — prosecutor follow-up on commit `7abb781` returned five findings (2 critical, 3 important). fixed all five and expanded the bug-class audit. C2: `run4_erasure_ablation` also inherited `alpha_log_mean=-0.5` — fixed to 2.2. C1: dead `alpha_log_std=0.1` removed from slot preset (SlotMemory never consumes that field). I1: math in `run2_slot_memory_decay_copy_paste.md` corrected from 0.993^256 ≈ 0.16 to 0.9933^256 ≈ 0.18. I2: structural guard `_assert_preset_retention_safe` added — raises at `_resolve_preset` whenever a preset with DELTA or SLOT layers does not override `alpha_log_mean`. I3 (as originally implemented): slot memory banner claimed the `structure_ratio=0.000` probe corroborated evaporation — **this claim was fabricated and was retracted in the next commit round, see the entry below**. **expanded audit revealed the bug class was wider than run 2**: `god` (legacy, empty overrides), `run1_baseline_noerasure`, and `run4_erasure_ablation` all inherited the broken default. `run1_baseline_noerasure` now explicitly sets `alpha_log_mean=2.2` so the guard permits it. `god` remains intentionally broken (deprecated) and will fail the guard on any future invocation, which is correct. the "four stacked failure modes" theory is downgraded from proven to provisional: every paid failure shared state evaporation, so the other three modes have never been tested in isolation.
- **2026-04-15** — deyan todorov — second-round prosecutor (on commit `f0e1a9a`) returned two important findings, both fixed in `ca968e3` + `699ba89`. I-A: the original I3 banner claim — that run 2's `delta_state_structure_probe` reported `mean_structure_ratio=0.000` as corroborating evidence of evaporation — was invented. the actual eval artifact at `neuroloc/output/run2_slot_memory/run2_slot_memory_eval_suite.json` contains `{"error": "no DELTA layers had populated state", "per_layer": {}}` and has no `mean_structure_ratio` key. the `0.000` in the step log is the logger's `.get(..., 0)` default when the key is absent. the probe does not run on a SLOT-only model because `run_delta_state_structure_probe` filters blocks by `layer_type == "DELTA"`. all three files (slot_memory_design.md banner, run2_slot_memory_decay_copy_paste.md, and the prior-runs index in this file at line 93) now report the artifact accurately and no longer claim probe-based corroboration. the evaporation diagnosis rests on the init math alone. I-B: `_assert_preset_retention_safe` had no regression test; added nine tests covering the guard's positive and negative paths plus a parametrized check that each named preset sets `alpha_log_mean`.
- **2026-04-15** — deyan todorov — third-round prosecutor on commits `ca968e3`, `dfb4d3a`, `699ba89` returned two important + one minor finding, all fixed. I-1: the I3 description in this very update history was itself inaccurate (it described the original invented claim as the fix); the second-round entry above has been corrected to mark the original I3 as retracted and this entry records the correction. I-2: the parametrized named-preset test was redundant because it went through `_resolve_preset` which runs the guard; it now calls `_resolve_preset_raw` to read the raw preset dict directly, and a separate `test_resolve_preset_wires_retention_guard_into_resolution_path` monkeypatches a missing-`alpha_log_mean` preset to prove the guard is wired into the resolution path. M-1: the mistake doc's causal claim about the probe's skip condition now cites the exact loop structure rather than paraphrasing, and hedges against the alternative reading where DELTA layers exist but carry no state.
- **2026-04-15** — deyan todorov — fourth-round prosecutor on commit `a23d4eb` returned one important finding, fixed by this entry. the third-round entry above claims "the second-round entry above has been corrected to mark the original I3 as retracted." that is factually wrong: the retraction annotation was added to the FIRST-round entry (line 161), not the second-round entry (line 162). the append-only rule on this section means the mistaken sentence stays where it is; this entry exists so the correction trail is accurate. the actual retraction annotation, the sentence "this claim was fabricated and was retracted in the next commit round, see the entry below", lives inside the first-round 2026-04-15 entry that describes the C1/C2/I1/I2/I3 fixes. no code change accompanies this entry; it is a purely documentary correction to the fourth-round prosecutor finding.
- **2026-04-15** — deyan todorov — paid h200 launch of `run2_slot_memory` with retention fixed (`alpha_log_mean=5.0`) attempted twice. first attempt killed at 17 min after only step 0 logged: throughput was 655 tok/s = 56 hours projected. root cause: `flash-linear-attention` was not pinned in `requirements.txt`, the pod did not have it installed, `FLA_AVAILABLE = False`, and `SlotMemory.use_fla = False`, so every slot layer fell through to the pure-python `_recurrent_slot_update` loop. fix in commit `edcfe5d` pinned `flash-linear-attention>=0.4.0` and `datasets>=2.19.0`. second attempt completed cleanly at 33,000 tok/s, 4000 steps, 72 min. final val_bpb 1.4777 (best, vs god_run_v2 1.4453 — slot ~2% worse on language modelling). eval suite started, reported passkey@256 = 0/100, passkey@1024 = 0/100, killed before passkey@4096 / selective_copy / structure_probe. retrieval_gate not persisted. user halted pod after the partial eval. mistake documented at `wiki/mistakes/run2_slot_memory_fla_silent_fall_through.md`. run card at `neuroloc/wiki/tests/run2_slot_memory_retention_fixed_results.md`.
- **2026-04-15** — deyan todorov — major realisation: four paid runs have produced 0% passkey across two substrates (matrix and slot) and two retention regimes (broken inherited and explicit alpha_log_mean=5.0). cpu gates A and B verified the slot mechanism retrieves perfectly when addresses are placed by hand. the consistent 0% under SGD on natural text is now interpreted not as architectural failure but as a category mismatch: the architecture is specified as a substrate for neural computation (memory, compression, thinking, imagination per `spec/blueprint.md` and `spec/next_gen.md`) but the training pipeline is a token predictor on natural text (fineweb-edu, cross-entropy on next byte). natural text rewards local distributional structure, which the four attention layers handle on their own; the 24 memory layers are unconstrained by the loss and collapse toward inactive (mean output gate 0.018 throughout the run). gradient descent never learns to route information through memory because the loss does not need memory routed. analysis at `wiki/synthesis/training_objective_vs_architectural_goal.md`. the next paid run must therefore train on a corpus that exercises the memory substrate (synthetic cognition curriculum, mixed pretraining, or two-phase pretraining are the three options laid out in that article). additionally, the project's competitive bar is the ~2-3 bits-per-parameter knowledge ceiling of vanilla transformers (allen-zhu et al. 2024); a successful paid run must demonstrate exceeding that bar, not just non-zero passkey. the "four stacked failure modes" theory from `linear_attention_retrieval_wall.md` is superseded by this realisation.
- **2026-04-15** — deyan todorov — count correction to the "major realisation" entry above. that entry was appended after `run2_slot_memory` (the fourth paid run, with broken inherited retention) but BEFORE `run2_slot_memory_retention_fixed` completed later the same day. the analysis is correct but the count is off by one: the realisation is supported by FIVE paid runs, not four. the fifth run (`run2_slot_memory_retention_fixed`) is what made the realisation testable in the first place — it was the only paid run with retention not in the evaporating regime, and it produced the same 0/100 passkey at 256 as the four prior runs. so the prior entry's claim "four paid runs have produced 0% passkey across two substrates and two retention regimes" should be read as "five paid runs ..." for any future agent. this entry is appended per the append-only rule rather than editing the prior one.
- **2026-04-17** — deyan todorov — phase-1 cognition training infrastructure committed. new module `neuroloc/data/cognition_corpus.py` generates synthetic byte corpora containing the three task primitives the paid architecture is supposed to exercise: passkey retrieval at varied distance (50% of blocks), key-value associative recall (30%), and sequence copy (20%). ASCII hex digit alphabet, distinct marker byte triples per task type, `generate_cognition_corpus` and `split_train_val` entry points, 11 unit tests covering determinism, mix-weight validation, alphabet restriction, and byte-level recall correctness. `neuroloc/model/god_machine.py` gains `NM_DATASET=cognition` option that routes through the new generator. new preset `run3_cognition_phase1` keeps the slot substrate and retention fix (alpha_log_mean=5.0) but replaces fineweb-edu with the cognition corpus. added to `gated_baseline_presets` so it inherits `NM_AUTHORIZE_FULL_RUN` enforcement, `retrieval_gate` persistence, and fail-closed exit on 0% passkey. also committed: structural guard `_assert_fla_available_if_requested` which fails loudly at startup if a preset requests FLA but `flash-linear-attention` is not importable on the runtime — closes the silent fall-through class documented in `wiki/mistakes/run2_slot_memory_fla_silent_fall_through.md`. escape hatch `NM_FORCE_NO_FLA=1` flips `use_fla_if_available` to False at runtime-override time for intentional non-FLA runs. `use_fla_if_available` removed from the canonical config hash since it is a runtime-environment knob, not a launch-contract property. full test suite passes (64 tests, 0 fail). smoke test exercises the new preset. the next paid run is this phase 1 — not yet launched; awaiting pod provisioning and explicit authorisation.
- **2026-04-17** — deyan todorov — **SIXTH paid run closed**. `run3_cognition_phase1` trained the slot substrate at 355m params on 131m tokens of synthetic cognition data at 33k tok/s for 72 min on h200. val_bpb plateaued at 6.3519 (the alphabet prior for the cognition corpus's byte distribution) from step 150 and never descended. partial eval before user stopped the pod reported passkey @ 256 = 0/100 and passkey @ 1024 = 0/100. this run was the predicted discriminant from `wiki/synthesis/training_objective_vs_architectural_goal.md`: the article proposed option 3 two-phase pretraining as the cleanest way to test "is the training objective the problem or is the architecture the problem?" the test executed; the answer is "the architecture cannot be trained by sgd at this configuration". new analysis article `wiki/synthesis/substrate_requires_architectural_change.md` catalogs the observation and proposes five candidate architectural interventions ranked by cost: (A) open slot output gate at init, (B) auxiliary retrieval loss on marker-following positions, (C) orthogonal prototype key init, (D) hand-placed-address warm start, (E) substrate replacement. run card at `wiki/tests/run3_cognition_phase1_results.md`. total paid cost across the six runs is ~$20 of h200 compute. architecture development remains paused; no further paid launch authorised until at least one of the candidate interventions is implemented and cpu-validated. "next paid run is another LM or cognition training on the current substrate" is specifically ruled out.
- **2026-04-17** — deyan todorov — **CURRICULUM PIVOT**. the project's active workstream is now the teaching PDF curriculum at `~/.claude/plans/compressed-dancing-haven.md`. paid compute is paused indefinitely; the architectural-intervention track (A-E candidates in `substrate_requires_architectural_change.md`) remains in the research backlog but is not active. the curriculum specifies 36 chapters across 6 phases (math fundamentals → biology fundamentals → computation in the brain → ML math → advanced architectures and compression → paper implementation) at 20-25 pages per chapter in English LaTeX, written in the pedagogical voice of `/Projects/dzipobel` adapted to English. chapter production uses parallel research agents (wiki + cited papers + classical textbooks + online resources + reference implementations + recent papers) with prosecutor validation on both the research output and the draft. the user executes chapter production themselves from the plan file. a new `## pivot to teaching curriculum (2026-04-17)` section was added above `## status` and a new `## curriculum track` section was added above `## experimental method` with the full 36-chapter outline. the `## status`, `## hypothesis under test`, `## experimental method`, and `## decision rules` sections remain as historical record for when the architectural-intervention track resumes.
- **2026-04-21** — deyan todorov — external benchmark synthesis added two new current articles: `wiki/synthesis/phase1_evaluation_surface_for_neural_models.md` and `wiki/synthesis/synthetic_shared_world_bridge.md`. the answer recorded there is that future cpu-first work should not use passkey or next-token loss as the phase-1 proxy. the minimal phase-1 battery should separately test recognition / belief-state formation, associative recollection, interference resistance, delayed use under partial observability, episodic reuse, and iterative reasoning / counterfactual rollout, with explicit trainability controls such as oracle-write and oracle-read ablations, gate-init sweeps, and slot-usage telemetry. for phase 2, the recommended bridge is a synthetic shared world with exact hidden state and multiple renderers (symbolic, image, toy audio, query surface), borrowing bias discipline from worldsense and the skill taxonomy from perception test, before any move to real audio-video or household embodied benchmarks. this does not change the curriculum pivot; it records the backlog methodology to use when the architectural-intervention track resumes.
- **2026-04-21** — deyan todorov — canonical backlog method updated to match the new synthesis above. `## current question`, `## immediate next action`, `## hypothesis under test`, `## experimental method`, the decision rules, and the prior-runs note now explicitly treat passkey as a smoke test inside a broader phase-1 battery rather than the sole launch gate. backlog work when the curriculum completes now starts with cpu-first NM evaluation and trainability controls, then the synthetic shared-world bridge, and only after that a paid intervention run.
- **2026-04-22** — deyan todorov — the phase-1 backlog method is now partially implemented in code and promoted into canonical project state. `neuroloc/simulations/` now contains the first biology-grounded symbolic battery for the backlog track: `slot_key_interference_sweep.py`, `multi_association_recall.py`, `delayed_cue_world.py`, `episodic_separation_completion.py`, `episodic_replay_reuse.py`, and `contextual_gate_routing.py`, wired into a dedicated `biology_phase1` suite and folded into `phase1_nm`. the project plan's `last updated`, `## current question`, `## immediate next action`, and `## experimental method` sections were revised to reflect that the remaining backlog work is no longer "define the battery" but "finish the latent-world deliberation probe and the model-side NM evaluation surface." the curriculum pivot remains the active workstream and no paid run is authorised.
- **2026-04-22** — deyan todorov — correction to the 2026-04-17 "phase-1 cognition training infrastructure committed" entry above. the phrase "full test suite passes (64 tests, 0 fail)" is historical and no longer reflects the current repository size. as of 2026-04-22, `python -m pytest tests --collect-only -q` collects 276 tests locally. the durable claim from the 2026-04-17 entry should be read as "the then-current local test surface passed and the new cognition-corpus path had tests," not as a current repo-wide count.
- **2026-04-23** — deyan todorov — compression-cluster continuity cleanup. `wiki/synthesis/compression_beyond_quantization.md`, `wiki/knowledge/generative_memory_research.md`, and `wiki/bridge/memory_compression_to_tiered_architecture.md` now have explicit thesis / literature / architecture-translation roles. `wiki/knowledge/compression_architecture.md` is no longer marked current; it is now the earlier proposal shelf superseded by the bridge article. this file's compression-track section and open questions were corrected to match the 2026-04-14 correction-field results already present in the append-only history: correction-field is a reconstruction-side effect, not a matrix-memory capacity gain.
- **2026-04-23** — deyan todorov — six-lane research-swarm and wiki-synthesis pass completed. raw artifacts now live at `neuroloc/output/research_swarm/2026-04-23/` with per-lane `harvest.md`, `challenge.md`, `synthesis.md`, `sources.md`, and `figures.md`, plus `merged_evidence_matrix.md`. the canonical wiki now includes new literature shelves (`knowledge/systems_neuroscience_research.md`, `knowledge/cellular_molecular_neurobiology_research.md`, `knowledge/cognitive_architecture_research.md`, `knowledge/cross_scale_building_blocks_research.md`, `knowledge/architectures_beyond_next_token_research.md`, and the three visual-source shelves), new synthesis pages on cellular primitives, cross-scale building blocks, working memory as controlled access, attention as precision and routing, world models and planning, beyond-next-token architectures, the visual grammar, and research implications, new bridge pages translating the same cluster into phase-1 tests, curriculum usage, replay, indexing, and routing, and a new comparison page `comparisons/dreamer_muzero_jepa_titans.md`. the glossary was extended to include `belief state`, `complementary learning systems`, `indexing theory`, `replay`, and `world model`. the canonical backlog theory is now sharper but not more urgent: multi-timescale state, indexing plus replay, routing and gating, branch-local computation, and latent state-action models are promoted as the highest-value theory cluster for the post-curriculum backlog. no part of this update authorises paid compute.
- **2026-04-23** — deyan todorov — research recovery and recent spiking-source check promoted `wiki/synthesis/indexed_reconstruction_compression.md` as the compact-memory proof surface after the correction-field null result. the new external sources are recorded narrowly: complemented-neuron ternary snns and neuronspark-0.9b support preserving membrane or subthreshold state around discrete events, but they do not prove retrieval capacity for the current model. the immediate-next-action field now records chapter 1 as review-ready after the math correction pass and chapter 2 as outline-only.
- **2026-04-23** — deyan todorov — approved neural-model research/test-material preparation plan added. the old todorov architecture is now treated as historical evidence; the neural model is the active scientific object. new synthesis article and nine mechanism dossiers define the pre-test research contract for local neuron state, memory formation, addressing, interference, compression, reconstruction, replay/rewrite, iterative rollout, and trainability. no paid compute or intervention selection is authorised by this update.
- **2026-04-24** — deyan todorov — first hard symbolic neural-model test-material package implemented. `neuroloc/data/nm_worlds.py` now exposes hard symbolic contracts and deterministic policies; `neuroloc/simulations/memory/nm_hard_symbolic_test_material.py` records the metrics artifact; `hard_symbolic_nm` is a new suite with smoke and hard profiles. the package covers all ten v1 gates, including reasoning, imagination/recombination, and compression. validation passed for 23 focused tests, both suite profiles, and repository collection now reports 290 tests. no paid compute, full-model integration, or intervention selection is authorised by this update.
- **2026-04-24** — deyan todorov — hard symbolic neural-model test-material package documented at `wiki/tests/hard_symbolic_nm_test_material.md`. the page records what the tests prove, what they do not prove, the validation record, and the next executable target: a tiny trainable neural-model mirror on the same latent worlds.
- **2026-04-25** — deyan todorov — neural-model compression-stack theory added at `wiki/synthesis/neural_model_compression_stack.md`. the compression target is now explicitly operation-preserving and stack-wide: cache, address, payload, episode, imagination, replay-rewrite, and world-state compression must be swappable codecs judged by task-relative rate-distortion. the next no-paid proof is oracle compression analysis on the hard symbolic worlds before the tiny trainable mirror.
- **2026-04-25** — deyan todorov — paper-spine refactor added `wiki/synthesis/neural_model_paper_spine.md` and `wiki/synthesis/oracle_compression_analysis_plan.md`. the current question is now oracle compression bounds on hard symbolic worlds, with novelty side-tracks treated as conditional proof packages rather than assumed claims. paid compute remains blocked.
- **2026-04-26** — deyan todorov — master plan refactor. this file is now the single canonical master plan for the neural model project: a biology-led, proof-gated neural world-memory model program organized as a flagship paper plus gated side-paper candidates and six compact research lanes. the next action is broad lane gap mapping beginning with cellular/local state storage and then oracle compression bounds. no paid compute, simulator choice, full-model integration, or architecture preset change is authorised.
- **2026-04-26** — deyan todorov — prosecutor consistency fix. stale references to the 2026-04-23 research/test-material phase were updated to the 2026-04-26 master-plan research phase so the curriculum and status sections agree with the new canonical plan.
- **2026-04-26** — deyan todorov — cellular-state research gap map added. `wiki/synthesis/cellular_state_storage_gap_map.md` records the first biology-led lane synthesis: fast carry state, eligibility or write-permission trace, and slow stability or control state are the highest-priority local-state hypothesis cluster; the next action is a mechanism dossier plus oracle compression analysis, not code or paid compute.
- **2026-04-27** — deyan todorov — first cellular mechanism dossier added. `wiki/synthesis/neural_model_dossier_eligibility_gated_local_commit.md` defines eligibility-gated local commit plus bounded output-capacity state, with required symbolic families, controls, telemetry, leakage risks, and kill conditions; next action moves to symbolic/oracle test material and oracle compression analysis.
- **2026-04-27** — deyan todorov — first cellular symbolic contract added. `wiki/synthesis/neural_model_symbolic_contract_eligibility_gated_local_commit.md` defines the episode fields, candidate/relevance/commit/read/exposure phase splits, controls, metrics, leakage checks, implementation handoff, and kill conditions for eligibility-gated local commit; next action moves to the no-paid symbolic generator and deterministic evaluators.
- **2026-04-27** — deyan todorov — first cellular symbolic/oracle test-material package implemented. `neuroloc/data/nm_worlds.py` now exposes the eligibility-gated local-commit generator and deterministic policies; `neuroloc/simulations/memory/eligibility_gated_local_commit.py` records the metrics artifact; `eligibility_commit` is registered as a simulation suite; documentation lives at `wiki/tests/eligibility_gated_local_commit_test_material.md`. validation passed 13 focused eligibility tests, 14 hard-symbolic/registry regression tests, and the `eligibility_commit` smoke suite. next action moves to oracle compression analysis, not paid compute or model integration.
- **2026-04-27** — deyan todorov — first oracle compression analysis package implemented. `neuroloc/simulations/memory/oracle_compression_analysis.py` records verbatim, latent-state, schema/residual, and imagined-branch bit accounts across `hard_symbolic_nm` and `eligibility_commit`; `oracle_compression` is registered as a suite; documentation lives at `wiki/tests/oracle_compression_analysis_results.md`. hard-profile validation covers 448 contracts, 14 families, two surfaces, operation preservation 1.0, controls preservation 1.0, leakage-free rate 1.0, accepted rate 0.5714, eight strong families, and six weak families below 10x. next action is the oracle-compression frontier split, not paid compute or model integration.
- **2026-04-27** — deyan todorov — oracle compression frontier split added. `wiki/synthesis/oracle_compression_frontier_split.md` separates the first oracle result into eight accepted frontier families and six weak frontier families. `compression_under_bit_budget` is ranked as the first narrow learned-codec proof-package candidate because it directly tests the compression claim. global tiny-mirror training, full-model integration, simulator work, and paid compute remain blocked.
- **2026-04-27** — deyan todorov — first narrow learned-codec proof package added. `wiki/synthesis/neural_model_dossier_compression_under_bit_budget_codec.md` defines the conditional learned-codec claim, non-oracle input rule, preserved operation, controls, metrics, telemetry, pass criteria, and kill conditions for the accepted `compression_under_bit_budget` frontier family. next action is a tiny local mirror contract for that family only; global mirror training, full-model integration, simulator work, and paid compute remain blocked.
- **2026-04-27** — deyan todorov — tiny local mirror contract added. `wiki/synthesis/neural_model_tiny_mirror_contract_compression_under_bit_budget.md` defines the authorized local implementation scope, deterministic train/validation/test splits, forbidden inputs, baselines, trainability splits, losses, telemetry schema, acceptance thresholds, and kill conditions for the first learned-codec mirror. next action is local code for this family only; full-model integration, simulator work, global mirror training, and paid compute remain blocked.
- **2026-04-27** — deyan todorov — first local compression mirror code surface added. `neuroloc/simulations/memory/compression_under_bit_budget_mirror.py` and `tests/test_compression_under_bit_budget_mirror.py` implement deterministic splits, forbidden-input guards, query-horizon observation trimming, exact evaluation-contract reuse, baseline controls, suite-registry wiring, explicit bit accounting, blocked-authorization maximum gates, and telemetry for `compression_under_bit_budget`; documentation lives at `wiki/tests/compression_under_bit_budget_mirror.md`. validation passed 10 focused tests, the compression-mirror smoke suite, py_compile, and repository collection now reports 317 tests. this is not learned compression yet; next action is the local learned-codec implementation on this surface.
- **2026-04-27** — deyan todorov — first local learned-codec smoke result added to `compression_under_bit_budget_mirror.py`. the trainable field codec uses non-oracle query-time observations only and emits learned rows, predicted state/action, measured compact code fields, bit accounting, control gaps, pass flags, and kill-condition telemetry. smoke validation passed 12 focused tests and repository collection now reports 319 tests. the result is negative: train joint success 1.0, validation joint success 0.0, test joint success 0.0, test action success 0.0625, learned compression ratio versus verbatim 3.25x, bits per successful held-out episode undefined, engineering pass 0.0, paper-track pass 0.0. next action is trainability-split and provenance-localization diagnostics, not paid compute or full-model work.
- **2026-04-27** — deyan todorov — diagnostic-localization controls added to the `compression_under_bit_budget` mirror. learned-code/oracle-decoder, oracle-code/learned-decoder, learned-address/oracle-payload, oracle-address/learned-payload, and provenance-exposed learned-codec rows are now diagnostic-only and excluded from learned-result counts. smoke validation localizes the failure toward payload/action/source-state inference and learned decoder generalization: learned-address/oracle-payload reaches 0.5 joint success, while the learned-payload and learned-decoder branches remain 0.0. next action is local mirror revision, not paid compute or full-model work.
- **2026-04-27** — deyan todorov — source-availability and decoder-generalization diagnostics added to the `compression_under_bit_budget` mirror. the smoke suite now exposes 22 policies and 240 diagnostic rows over 24 records. the result is a sharper negative: source event observed rate 0.25, required source fields visible rate 0.0, source state reconstructable rate 0.0, visible-source-state/oracle-action/oracle-decoder joint success 0.0, and oracle-code/learned-decoder train 1.0 but validation/test 0.0. validation passed 19 focused tests, 21 targeted compression/registry tests, the compression-mirror smoke suite, py_compile, yaml parse, and repository collection now reports 328 tests. next action is to repair the family/mirror source-observability contract before another codec is trained.
- **2026-04-28** — deyan todorov — repaired the `compression_under_bit_budget` source-observability contract and mirror diagnostics. the symbolic family now exposes the committed source event plus adjacent velocity evidence before query time, and the action target is a function of visible source state rather than hidden identity. the smoke suite now exposes 23 policies and 264 diagnostic rows over 24 records. source event observed rate, required source fields visible rate, source state reconstructable rate, visible-source-codec joint success, and visible-source-state/oracle-action/oracle-decoder joint success are all 1.0. the learned codec still scores train joint success 1.0 but validation/test joint success 0.0; latest learned smoke metrics are address 0.25, color 0.25, shape 1.0, position 0.25, velocity 0.0, action 0.0, and provenance 1.0. the next no-paid target is learned address/payload/action and decoder generalization, not paid compute or full-model work.
- **2026-05-02** — deyan todorov — linux migration handoff added at `docs/linux_handoff_2026-05-02.md` as an operational supplement. this records the transfer state, validation commands, user preferences, mistake history, current compression-mirror metrics, and next local no-paid work for the linux continuation environment without changing the canonical scientific question.
- **2026-05-06** — deyan todorov — subq / selective sparse attention related-work boundary added. [[content_routed_sparse_read_prior]] records what the public subq material validates for the project, what it does not prove, and why future compression claims must compare against content-routed sparse read over verbatim context where that baseline is fair. the immediate no-paid learned-codec repair target is unchanged.
- **2026-05-06** — deyan todorov — multi-lane related-work pressure matrix added. [[neural_model_related_work_pressure_matrix]] consolidates six research sweeps into a prior-art boundary and next-step standard across local cellular state, operation-preserving compression, memory/replay/imagination, simulator selection, trainability/evaluation, and content-routed sparse-read baselines. no paid compute or model integration is authorised.
- **2026-05-07** — deyan todorov — content-routed sparse-read baseline added to the `compression_under_bit_budget` mirror. [[tests/compression_under_bit_budget_mirror]] now records that legal sparse read solves the repaired smoke task from two selected observation records but exceeds the compact-code budget. the next local target is learned-codec repair against this baseline or proof-package demotion.
- **2026-05-07** — deyan todorov — original `compression_under_bit_budget` source-pair mirror demoted as compression evidence after external review. matched-budget sparse read and a distributed-evidence probe were added to [[tests/compression_under_bit_budget_mirror]]; next target is learned compact-code operation preservation on distributed evidence, starting with oracle-code/learned-decoder generalization.
- **2026-05-07** — deyan todorov — tiny distributed local learned model added to [[tests/compression_under_bit_budget_mirror]]. the model trains locally on distributed evidence, reaches 1.0 test joint/state/action success at 19 committed bits, and beats matched-budget sparse read at 20 bits and 0.0 joint success. this is recorded as a narrow local symbolic positive only; next step is stronger local falsification, not paid compute.
- **2026-05-07** — deyan todorov — factor-heldout local falsification gate added to [[tests/compression_under_bit_budget_mirror]]. the ordinary-split tiny model fails held-out color-shape pair bands with test joint success 0.03125 and engineering pass 0.0, despite seen individual colors and shapes. next step is local compositional-generalization repair, not paid compute.
- **2026-05-07** — deyan todorov — 10k-scale local data-heavy robustness gate added to [[tests/compression_under_bit_budget_mirror]]. after prosecutor review removed an evaluator-source shortcut from the encoder path, legal field-specific input projections plus small per-field nonlinear heads recover four heldout factor axes across three smoke seeds at test joint success 1.0 with 9,792 parameters, 24,576 generated local train records, 19 committed bits, and useful operation success per committed bit 0.05263157894736842. this is useful-state-density evidence on one symbolic surface, not a general message model and not solved compression. the follow-up bridge entry below supersedes its immediate next-step wording.

- **2026-05-07** — deyan todorov — constrained generated-language bridge added at [[tests/language_grounded_state_density_mirror]]. the first bag-of-words attempt failed because flat token counts destroyed time-slot relations; the accepted bridge preserves event, time, slot, and observed-field structure from generated text. the 3,847-parameter local model clears four heldout axes across two smoke seeds with 16,384 generated train records, 768 validation records, 768 test records, 19 committed bits, and useful operation success per committed bit 0.05263157894736842. this is constrained message-response evidence only, not arbitrary chat, not solved compression, and not paid-compute authorization.
- **2026-05-07** — deyan todorov — parser-resistant local-state gate added to [[tests/language_grounded_state_density_mirror]]. randomized prompts remove stable field prefixes, shuffle event order, inject irrelevant clauses, report parser/schema cost, and force the learned path through bounded local state with zero-state and shuffled-state ablations. the learned token-count encoder fails with minimum test joint/state success 0.0, while matched-budget sparse read remains 0.0 and uncapped sparse read remains 1.0. the bridge is downgraded to structured symbolic-message evidence; next local target is a learned sequence or event-binding encoder under the same controls.
- **2026-05-07** — deyan todorov — event-binding parser-supported foundation added to [[tests/language_grounded_state_density_mirror]]. the new local path keeps randomized prompt templates, removed stable prefixes, shuffled event order, irrelevant clauses, matched-budget sparse read, uncapped sparse read, zero-state ablation, and shuffled-state ablation, but preserves event segments as bounded local state before answering. it reaches minimum test joint/state/action success 1.0 across four heldout axes and two smoke seeds, with matched-budget sparse read 0.0 at 20 bits, zero-state joint max 0.0, shuffled-state joint max 0.020833333333333332, 19 state bits plus 37 parser/schema bits for 56 accounted bits, parser-supported foundation pass 1.0, and trainable engineering pass 0.0. this is the first constrained grounded parser-supported state responder baseline, not a learned language model, not arbitrary chat, not solved compression, and not paid-compute authorization.
- **2026-05-07** — deyan todorov — typed trainable event-binding local-state gate added to [[tests/language_grounded_state_density_mirror]]. after the deterministic event-binding parser baseline and the failed unconstrained shared learner, field-specific typed segment heads now clear the randomized symbolic-message gate with 8,856 parameters, minimum test joint/state/action success 0.9583333333333334, field floor 0.9583333333333334, 19 state bits plus 37 parser/schema bits for 56 accounted bits, zero-state joint max 0.0, shuffled-state joint max 0.020833333333333332, engineering pass 1.0, and no paid compute. this remains constrained local-state evidence, not arbitrary chat or solved compression.
- **2026-05-08** — deyan todorov — first dataset-grounded local v1 language-state gate added at [[tests/local_v1_language_model]]. `neuroloc/simulations/memory/local_v1_language_model.py` trains a 2,688-parameter state-first router over local dataset-derived memory records, answers from bounded state rather than a next-token decoder, and reaches smoke minimum joint/state/action/provenance success 1.0 with zero-state and shuffled-state collapse. the same surface now includes constrained memory update, targeted replay, random-replay failure, and branch-state rehearsal controls with dialogue engineering pass 1.0. this is the first laptop-only local v1 responder surface, not arbitrary chat, open-ended imagination, solved compression, or full neural-model v1.
- **2026-05-08** — deyan todorov — first integrated 10k local chat surface added at [[tests/local_10k_chat_model]]. `neuroloc/simulations/memory/local_10k_chat_model.py` trains a 7,968-parameter state-first router over dataset-derived memory records, exposes command-line chat, refuses unknown prompts, updates memory, carries short-term dialogue state, performs targeted replay, and creates bounded branch-state rehearsal. smoke validation reports 32 records, two seeds, 61 accounted bits, grounded response success 0.96875, unknown refusal success 1.0, route-disabled grounded success 0.03125, shuffled-route grounded success 0.0, the remaining local chat operation metrics at 1.0, and engineering pass 1.0. this is the current runnable laptop v1 chat foundation, not arbitrary chat, solved compression, full-scale language competence, or paid-compute authorization.
- **2026-05-08** — deyan todorov — local v1 cue-binding hardening added after the expanded canonical docs changed the default dataset and introduced short common status-word cues. `local_v1_language_model.py` now resolves cue-bearing prompt patterns before falling back to vector routing, restoring minimum joint/state/action/provenance success 1.0 in the expanded `compression_mirror` smoke suite. the updated local v1 surface uses 2,720 trainable parameters and remains subordinate to the current `local_10k_chat_model` artifact.
- **2026-05-08** — deyan todorov — first under-10k architecture-boundary foundation surface added at [[tests/local_foundation_neural_model]]. `neuroloc/simulations/memory/local_foundation_neural_model.py` trains a 5,560-parameter state-first router over encoded memory objects, exposes context state, working memory, episodic memory, long-term memory, replay buffer, branch memory, and the replaceable `schema_residual_identity_v0` codec boundary, and clears grounded answer, action, update, context, targeted replay, branch-state, final dialogue, route-disabled, shuffled-route, and codec-disabled controls. this is the current laptop foundation surface, not arbitrary chat, solved compression, full-scale language competence, or paid-compute authorization.
- **2026-05-08** — deyan todorov — category-error audit added at [[mistakes/local_foundation_lookup_scaffold_category_error]]. the local v1, local 10k, and local foundation surfaces are demoted to scaffolds: their routed record tables, fixed renderers, targeted retrieval paths, and branch-copy paths do not prove memory, replay, imagination, compression, chat, or neural-model v1. `AGENTS.md` now contains hard gates for these terms. the next no-paid action is a real local v1 architecture contract before more implementation.
- **2026-05-08** — deyan todorov — foundation replay and branch dependence hardened after prosecutor review. replay responses now render from `replay_buffer`, branch responses now render from `branch_memory`, and `disabled_replay_buffer` plus `disabled_branch_memory` controls are registered, tested, and required to collapse their operation success to 0.0. this keeps the foundation artifact honest: the explicit surfaces are not only presence counters.
- **2026-05-08** — deyan todorov — canonical plan and researcher-workflow governance updated. the next phase is a non-executed local v1 mechanism-contract plan covering the full brain-stack foundation, with compression/useful-state density, replay, and imagination treated as core contract mechanisms rather than implemented claims. `AGENTS.md` now enforces the single-plan rule, researcher-style evidence logging, roadblock research-agent policy, and scaffold/promotion guardrails. no code, model training, paid compute, simulator choice, or research execution is authorised by this update.
- **2026-05-09** — deyan todorov — local implementation reactivated by explicit instruction. the 2026-05-08 planning-only blocker is removed for local code and simulation; active work is compression-first implementation toward an actual under-10k model, with wiki-first recovery, controls, telemetry, and documented evidence required before any stronger claim.
- **2026-05-09** — deyan todorov — active local model target raised to 100k parameters for coherence. the prior under-10k target remains useful as historical scaffold pressure, but the actual local model path is now compression-first 100k with the same wiki-first recovery, controls, telemetry, and category-error guardrails.
- **2026-05-09** — deyan todorov — learned compact-state write/read/update component mirror added to [[tests/local_state_write_read_mirror]]. registered smoke validation passes across four factor-heldout axes with 37,719 parameters, 56 accounted bits, 0.96875 minimum joint/state/action success, 1.0 update success, and zero matched-budget sparse-read success. next step is replay or rewrite plus constrained language-answer decoding, not final-model promotion.
- **2026-05-09** — deyan todorov — first finished local 100k symbolic-language model candidate added to [[tests/local_100k_replay_answer_mirror]]. registered smoke validation passes across four factor-heldout axes with 89,877 parameters, 56 accounted bits, learned event binding, compact-state write/read/update, delayed reactivation, rewrite, branch rollout, bounded answer decoding, 0.96875 minimum initial/replay/rewrite success, 1.0 branch rollout success, and explicit failed controls. next step is hard-profile validation and exact-state 3d grounding.
- **2026-05-09** — deyan todorov — hard-profile validation passed for [[tests/local_100k_replay_answer_mirror]] after repairing a generated-language surface bug where positions 21-30 wrapped through a 21-word number vocabulary. hard now passes with 98,817 parameters, 60 accounted bits, 1.0 initial and replay success, 0.96875 rewrite success, 0.984375 branch rollout success, and engineering pass 1.0. next step is exact-state 3d grounding.
- **2026-05-09** — deyan todorov — local exact-state 3d nm candidate added at [[tests/local_100k_3d_nm_mirror]]. registered smoke and hard validation pass with deterministic synthetic 3d worlds, 66,559 hard maximum parameters, 51 accounted bits, 1.0 initial world-state/object-permanence/occluded-localization/action-consequence/replay/exact-transition success, 0.9666666666666667 rewrite success, provenance preserved, and all required controls collapsed. [[mistakes/local_100k_3d_branch_rollout_overfit]] records the failed learned branch-transition attempt and the hard-seed rewrite failure discovered during implementation.
- **2026-05-09** — deyan todorov — local full small nm candidate added at [[tests/local_100k_full_nm]]. registered smoke and hard validation pass with one trainable module, 81,070 hard maximum parameters, 24 learned latent bits, 20 fixed bridge/schema/answer bits, 44 accounted bits, 1.0 initial world-state/object-permanence/occluded-localization/action-consequence/replay/rewrite/learned-branch-transition/bounded-answer success, useful density above the 51-bit 3d baseline, broad full-model authorization held at `0.0`, and all required controls collapsed. [[mistakes/local_100k_full_nm_soft_code_false_pass]] records the rejected soft-code path that failed hard-bit compression evaluation.
- **2026-05-09** — deyan todorov — high-density neuron-cell side proof added at [[tests/local_100k_high_density_cell]] after the related-work pressure update at [[high_density_neuron_cell_related_work_pressure_matrix]]. registered smoke and hard validation pass as a params-only partial candidate: hard reaches 4,096 exact facts, 8 trainable parameters, 114,688 useful retrievable bits, params-only multiplier 5,734.4x, and exact retrieval success 1.0, while strict density reaches only 6.1709981167608285x after charging committed state. [[mistakes/local_100k_high_density_cell_strict_600x_not_met]] records why the strict 600x breakthrough claim is rejected.
- **2026-05-09** — deyan todorov — schema-density attempt [[tests/local_100k_schema_density_cell]] demoted by [[mistakes/schema_density_cell_structured_target_category_error]]. although the registered simulation passes as formula compression over structured schema-generated facts, deyan rejected the target because structured means planned by construction and does not satisfy the requested high-density knowledge-compression claim. next work returns to unstructured or unknown-structure facts with full accounting and no hidden tables.
- **2026-05-09** — deyan todorov — unstructured exact-fact entropy probe added at [[tests/local_100k_unstructured_density_cell]]. hard validation passes as a negative result: 4,096 random-label facts, 122,880 useful bits, 1,218 committed state bits under the 600x budget, entropy lower bound 122,880 bits, entropy budget gap 121,633.28 bits, exact retrieval success 0.0, information-theoretic 600x possible 0.0, and breakthrough authorization 0.0. [[mistakes/unstructured_exact_600x_entropy_wall]] records the boundary. next work must use unknown-structure real data with a random-label twin, not independent entropy and not planned formulas.
- **2026-05-09** — deyan todorov — unknown-structure corpus boundary probe added at [[tests/local_100k_unknown_structure_density_probe]]. hard validation passes as a baseline result: 4,096 exact non-generated local wiki corpus chunks, 1,048,576 useful bits, 481,282 committed state bits after compressed payload, decoder, and manifest charges, exact retrieval success 1.0, random-label twin success 0.0, strict multiplier 13.941917871967359x, strict 600x pass 0.0, and breakthrough authorization 0.0. next work is a learned unknown-structure associative cell that beats this charged corpus-codec baseline plus sparse-read/product-key/verbatim controls.
- **2026-05-12** — deyan todorov — learned unknown-structure residual cell added at [[tests/local_100k_learned_unknown_structure_density_cell]]. hard validation passes as a hard defeat: source-heldout exact retrieval works, cross-label random scoring is 0.0, but a separately built random-label twin stores at 1.0; strict multiplier is only 3.0525410753623334x, the prior charged corpus-codec baseline remains 13.941917871967359x, and per-fact residual/key rows block promotion. [[mistakes/learned_unknown_structure_residual_table_defeat]] records the failure mode.
- **2026-05-12** — deyan todorov — dedicated compression research corpus added under `neuroloc/compression/`. it records in-repo compression attempts, modern memory/compression related work, open-source and passion-project baselines, theory limits, required baseline stack, and the first non-row shared-predictor exact codec product spec. the immediate next action is to implement that probe against the charged corpus-codec, minimal-perfect-hash, sparse-read, product-key, hdc/sdm, and random-label controls.
- **2026-05-12** — deyan todorov — shared-predictor exact-codec product added at [[tests/local_100k_shared_predictor_exact_codec]]. registered focused tests, registry contract, smoke, and hard validation pass. hard reaches 4,096 source-heldout exact facts, one block value stream, no per-fact value slices, exact retrieval success 1.0, random-label payload bits 1,048,944 versus real payload bits 388,864, controls collapse 1.0, strict multiplier 4.352614012398447x, and product pass 1.0. breakthrough authorization stays 0.0 because it loses to the 13.941917871967359x charged corpus-codec baseline and to the mph-payload baseline.
- **2026-05-12** — deyan todorov — source-block exact-codec product added at [[tests/local_100k_source_block_codec]]. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves 4,096 source-heldout chunks exactly from one compressed source block, reads from compressed block is 1.0, raw source block retained is 0.0, random-label twin success is 0.0, controls collapse is 1.0, strict accounted bits are 476,958, strict multiplier is 14.06876726917481x, and beats charged codec baseline is 1.0. breakthrough authorization stays 0.0 because the product uses source/offset query fields and does not prove arbitrary opaque-key high-density knowledge compression.
- **2026-05-12** — deyan todorov — source-block raw-cache mistake documented at [[mistakes/source_block_codec_raw_cache_category_error]]. an earlier implementation retained a decoded raw source-block cache while charging only the compressed block stream. the accepted product removes the raw cache, reads by decompressing the charged block stream on demand, reports reads from compressed block 1.0 and raw source block retained 0.0, and has tests asserting no decoded block remains.
- **2026-05-12** — deyan todorov — content-addressed source-codec product added at [[tests/local_100k_content_addressed_source_codec]]. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves 4,096 source-heldout chunks exactly from one compressed source block using 16-bit content digest handles instead of source/offset query fields. source-offset routing is 0.0, key assignment bits are 0.0, selected digest collision count is 0.0, controls collapse is 1.0, strict accounted bits are 476,954, strict multiplier is 14.06888524576417x, and beats source-block baseline is 1.0. breakthrough authorization stays 0.0 because the key is content-derived and does not prove arbitrary opaque-key high-density knowledge compression.
- **2026-05-12** — deyan todorov — llm-facing semantic qa exact-codec product added at [[tests/local_100k_llm_semantic_qa_codec]]. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves 4,096 source-heldout answers exactly from one compressed source block using bounded natural-language questions and token-signature handles. source-offset routing is 0.0, content-digest key target is 0.0, assignment row count is 0.0, per-fact value row count is 0.0, random-label twin success is 0.0, controls collapse is 1.0, strict accounted bits are 476,938, strict multiplier is 14.06935717190861x, and beats content-addressed baseline is 1.0. breakthrough authorization stays 0.0 because the handle is lexical, not learned semantic recall, and the minimal-perfect-hash payload line remains slightly stronger.
- **2026-05-12** — deyan todorov — weight-carried qa exact-codec product added at [[tests/local_100k_weight_carried_qa_codec]]. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves 4,096 source-heldout answers exactly from a compressed payload carried inside torch module state, removes stored manifest cost, reports external payload store 0.0 and stored manifest 0.0, tests adapter recompression update success 1.0 and state-dict reload success 1.0, collapses random-label and disabled controls, charges 441,064 strict bits, reaches strict multiplier 15.215221373768888x, and beats both the prior llm-facing qa baseline and the matched minimal-perfect-hash payload baseline. breakthrough authorization stays 0.0 because the handle is still lexical and base-weight implicit storage is not proved.
- **2026-05-12** — deyan todorov — paper-ready adapter benchmark added at [[tests/local_100k_paper_ready_adapter_benchmark]]. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves 4,096 source-heldout answers exactly across four source domains from a compressed payload carried inside torch module state, wraps the adapter in transformer and recurrent/state-style host modules, preserves state-dict reload, accepts paraphrased bounded questions, tests adapter recompression update success 1.0, collapses random-label and disabled controls, charges 403,256 strict bits and 407,352 paper-surface bits, reaches adapter strict multiplier 16.641752137599937x and paper-surface strict multiplier 16.474416229698146x, and satisfies the five paper-facing local gates. strict breakthrough authorization stays 0.0 because the handle is still token-signature based and base-weight implicit storage is not proved.
- **2026-05-13** — deyan todorov — margin recompression adapter added at [[tests/local_100k_margin_recompression_adapter]], and source-holdout overlap mistake documented at [[mistakes/paper_ready_adapter_source_holdout_overlap]]. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves 4,096 answers exactly from a disjoint stable four-domain compressed payload in torch module state, carries a trained four-parameter recompression update controller, reports false-hit rates 0.0, charges 295,144 strict bits and 299,240 paper-surface bits, reaches adapter strict multiplier 22.732738950163952x and paper-surface strict multiplier 22.421639537059313x, and satisfies the five paper-facing local gates. strict breakthrough authorization stays 0.0 because the handle is still token-signature based and the executable same-block content-scan diagnostic is not beaten.
- **2026-05-13** — deyan todorov — semantic alias payload adapter added at [[tests/local_100k_semantic_alias_payload_adapter]] and demoted by [[mistakes/semantic_alias_payload_adapter_formula_alias_category_error]]. registered focused tests, registry contract, smoke, and hard validation pass as a diagnostic: hard reaches exact answer success 1.0, block payload bits 258,144, committed state bits 290,952, paper-surface multiplier 22.7450665654402x, and lexical parser success 0.0, but the fair same-interface alias content scan succeeds at 1.0. publishable breakthrough authorization and strict breakthrough authorization are therefore fixed at 0.0, and future candidates must give same-interface scans the same parser/handle surface before any baseline win is claimed.
- **2026-05-13** — deyan todorov — source-native relation adapter added at [[tests/local_100k_source_native_relation_adapter]] and demoted by [[mistakes/source_native_relation_stride_rule_category_error]]. registered focused tests, registry contract, and hard validation pass as a diagnostic: hard reaches exact and paraphrase success 1.0, source train/test overlap counts 0.0, paper-surface multiplier 22.744449867143864x, and relationless scan success 0.0, but a fair stride-aware content scan succeeds at 1.0. publishable relation breakthrough authorization and strict breakthrough authorization are fixed at 0.0.
- **2026-05-13** — deyan todorov — weight-mantissa payload adapter added at [[tests/local_100k_weight_mantissa_payload_adapter]] and demoted by [[mistakes/weight_mantissa_payload_steganographic_accounting_error]]. registered focused tests, registry contract, and hard validation pass as a diagnostic: exact retrieval from fp32 parameter mantissas reaches 1.0 and controls collapse, but honest fp32-state accounting gives paper-surface density 16.94360217334222x while same-block content scan remains 23.06526987269378x. publishable weight-payload authorization and strict breakthrough authorization are fixed at 0.0.
- **2026-05-13** — deyan todorov — compression claim-surface audit fixed two overclaim gates after reviewer findings. `local_100k_margin_recompression_adapter` now reports bounded adapter engineering pass `1.0` but paper-ready candidate `0.0` and static compression publishable candidate `0.0` because content scan and mph are not beaten. `local_100k_source_native_relation_adapter` now reports legacy static public baseline pass `1.0` but static public baseline pass `0.0` because the fair stride-aware scan succeeds at `1.0`.
- **2026-05-13** — deyan todorov — narrow source-code block compression product added at [[tests/local_100k_indent_token_block_codec]]. registered focused tests, registry contract, smoke, and hard validation pass. hard reconstructs `99761` held-out source bytes exactly from a charged compressed stream, learns a four-byte indentation token from disjoint train files, reports path/hash train-test overlap counts `0.0` and sliding 64-byte ngram overlap count `3104.0`, charges token bytes plus a `144` bit token-map header, improves strict charged bits over the best same-block standard codec by `0.020192022171632188`, and shows random-label bytes receive negative compression gain. source-block codec product authorization is `1.0`; source-block breakthrough, high-density knowledge, static retrieval, chat, full nm, and strict general breakthrough authorization remain `0.0`.
- **2026-05-13** — deyan todorov — source-structure block codec added at [[tests/local_100k_source_structure_block_codec]] and promoted as the current narrow source-code byte-compression product. registered focused tests, registry contract, smoke, and hard validation pass. hard reconstructs `99761` held-out source bytes exactly from charged compressed indentation-count and body streams carried in a reloadable torch module state dict, learns a four-space indentation unit from disjoint train files, charges a `256` bit structure header, improves strict charged bits over the best same-block standard codec by `0.028555874492724932`, beats the indent-token product by `0.008363852321092744` strict margin, and shows random-label bytes receive negative compression gain. source-block codec product authorization is `1.0`; source-block breakthrough, high-density knowledge, static retrieval, chat, full nm, and strict general breakthrough authorization remain `0.0`.
- **2026-05-13** — deyan todorov — source-structure qa adapter added at [[tests/local_100k_source_structure_qa_adapter]]. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves `4096` source-heldout answers exactly from source-structure compressed count/body streams carried inside torch module state, passes transformer/recurrent host probes, false-hit controls, random-label collapse, trained recompression update, and state-dict reload, charges `279136` strict bits, reaches `24.03612607449857x` adapter strict multiplier and `23.688602733536655x` paper-surface multiplier, and beats raw content scan plus raw undercharged mph diagnostics. static breakthrough authorization stays `0.0` because the fair same-structure content scan remains slightly stronger at `24.041637051473117x`.
- **2026-05-13** — deyan todorov — source-token-structure block codec added at [[tests/local_100k_source_token_structure_block_codec]] and promoted as the current narrow source-code byte-compression product. registered focused tests, registry contract, smoke, and hard validation pass. hard reconstructs `99761` held-out source bytes exactly from charged count-delta, body-token, and dictionary streams carried in a reloadable torch module state dict, charges an `896` bit token-structure header, lowers payload bits from the source-structure product's `124200` to `123088`, improves strict charged bits over the best same-block standard codec by `0.03543501930119766`, beats the source-structure product by `0.00687914480847273` strict margin, and shows random-label bytes receive negative compression gain. source-block codec product authorization is `1.0`; source-block breakthrough, high-density knowledge, static retrieval, chat, full nm, and strict general breakthrough authorization remain `0.0`.
- **2026-05-13** — deyan todorov — source-subtoken-structure block and corpus codecs added at [[tests/local_100k_source_subtoken_structure_block_codec]] and [[tests/local_100k_source_subtoken_structure_corpus_codec]]. registered focused tests, registry contract, smoke, and hard validation pass. the block codec reconstructs `99761` held-out source bytes exactly, lowers charged payload bits from the source-token product's `123088` to `120952`, improves strict charged bits over the best same-block standard codec by `0.048648916163515785`, and keeps random-label payload gain negative. the frozen-manifest corpus codec reconstructs five source-code blocks exactly, verifies block hashes, charges selectors and fallback headers, lowers aggregate selected payload bits from `849752` to `812688`, reaches aggregate payload improvement `0.043617431909545375`, and requires random-label/control collapse. source-code block and corpus product authorization is `1.0`; source-block breakthrough, high-density knowledge, static retrieval, chat, full nm, and strict general breakthrough authorization remain `0.0`. the older token-corpus benchmark is demoted to diagnostic authorization `0.0` because its live-corpus discovery and weak controls were not product-grade.
- **2026-05-13** — deyan todorov — source-subtoken qa adapter added at [[tests/local_100k_source_subtoken_qa_adapter]] and promoted as the current strongest bounded exact qa adapter product. registered focused tests, registry contract, smoke, and hard validation pass. hard retrieves `4096` source-heldout answers exactly from source-subtoken compressed count/body/dictionary streams carried inside torch module state, passes transformer/recurrent host probes, false-hit controls, random-label collapse, trained recompression update, and state-dict reload, charges `277248` strict bits and `281344` paper-surface bits, reaches `24.19976921301639x` adapter strict multiplier and `23.847532408460314x` paper-surface multiplier, and beats raw content scan plus raw undercharged mph diagnostics. static breakthrough authorization stays `0.0` because the fair same-subtoken content scan remains slightly stronger at `24.20535549399815x`.
- **2026-05-13** — deyan todorov — zstd trained-dictionary public baseline audit added at [[tests/local_100k_zstd_trained_dictionary_baseline_audit]]. registered focused tests, registry contract, smoke, and hard validation pass. hard trains dictionaries only on target-excluded local source files, charges dictionary bytes plus header and selector bits, and compares both charged and undercharged diagnostic lines. the current source-subtoken block payload beats the charged public dictionary by `30424` bits and the undercharged payload-only line by `26248` bits. the frozen-corpus source-subtoken payload beats the charged public dictionary by `170152` bits and the undercharged payload-only line by `137304` bits. random-label dictionary gains remain negative, and path/hash train-test overlap counts are `0.0`. this strengthens the source-code byte-compression products but does not authorize strict breakthrough, broad knowledge, chat, full nm, or 600x claims.
- **2026-05-13** — deyan todorov — source-subtoken shared-dictionary corpus codec added at [[tests/local_100k_source_subtoken_shared_dictionary_corpus_codec]] and promoted as the current broader source-code corpus product. registered focused tests, registry contract, smoke, and hard validation pass. hard reconstructs five frozen-manifest source-code blocks exactly with one charged `112` token shared dictionary plus charged `16` token local dictionaries per block, lowers aggregate selected payload bits from the prior source-subtoken corpus product's `812688` to `803400`, raises aggregate payload improvement over the same-block standard sweep to `0.05454767979363391`, beats the charged zstd trained-dictionary baseline by `179440` bits, beats the undercharged zstd payload-only diagnostic by `146592` bits, and keeps random-label payload gain negative. source-code corpus product authorization is `1.0`; source-code breakthrough, high-density knowledge, static retrieval, chat, full nm, strict general breakthrough, and 600x authorization remain `0.0`.
- **2026-05-13** — deyan todorov — source-subtoken global-stream corpus codec added at [[tests/local_100k_source_subtoken_global_stream_corpus_codec]] and promoted as the current broader source-code corpus product. registered focused tests, registry contract, smoke, and hard validation pass. hard reconstructs five frozen-manifest source-code blocks exactly with one charged `256` token shared dictionary plus charged global count, body, and length/framing streams, lowers aggregate selected payload bits from the prior shared-dictionary corpus product's `803400` to `699144`, raises aggregate payload improvement over the same-block standard sweep to `0.1772375940274339`, beats the stronger global raw standard baseline by `37360` bits, beats the charged zstd trained-dictionary baseline by `283696` bits, beats the undercharged zstd payload-only diagnostic by `250848` bits, and keeps random-label payload gain negative. source-code corpus product authorization is `1.0`; source-code breakthrough, high-density knowledge, static retrieval, chat, full nm, strict general breakthrough, and 600x authorization remain `0.0`.
- **2026-05-14** — deyan todorov — source-subtoken global-stream corpus codec at [[tests/local_100k_source_subtoken_global_stream_corpus_codec]] now carries its charged payload in torch module state and reconstructs exactly after `state_dict` reload. focused tests, registry contract, smoke, hard, compile, and broader regression validation pass. this closes the model-state integration gap for the current broader source-code corpus product without promoting source-code breakthrough, high-density knowledge, static retrieval, chat, full nm, strict general breakthrough, or 600x authorization.
- **2026-05-14** — deyan todorov — source-subtoken disjoint retrieval codec added at [[tests/local_100k_source_subtoken_disjoint_retrieval_codec]] and promoted as the current narrow source-code exact-retrieval product. registered focused tests, registry contract, smoke, hard, compile, and broader regression validation pass. hard retrieves `14715` aligned 32-byte chunks exactly from disjoint frozen source blocks after torch `state_dict` reload, uses zero train path/hash overlap, charges `431536` retrieval bits, beats executable raw content scan by `19592` bits, beats an undercharged mph diagnostic by `19608` bits, and keeps random-label/control gates passing. source-code retrieval codec product authorization is `1.0`; source-code retrieval breakthrough, static breakthrough, high-density knowledge, chat, full nm, strict general breakthrough, and 600x authorization remain `0.0`.
- **2026-05-14** — deyan todorov — paq8px v214 public context-mixing scratch audit recorded in [[mistakes/public_context_mixing_baseline_missing]]. level 2 compresses the hard raw joined source to `50712` bytes, or `405696` payload bits, beating the current `699144`-bit global-stream corpus payload. source-code byte-compression claims now require paq8px pressure, and the next probe moves from relation qa to a source transform, grammar/phrase model, or entropy-model path that can beat this public line under matched accounting.
- **2026-05-14** — deyan todorov — paq8px public baseline run card added at [[tests/paq8px_public_context_mixing_baseline_audit]] so the public context-mixing pressure line is preserved as test evidence, not only as a mistake note.
- **2026-05-14** — deyan todorov — dense source-authored relation diagnostic added at [[tests/local_100k_source_dense_authored_relation_diagnostic]], then corrected after [[mistakes/dense_relation_signature_query_leakage]]. registered focused tests, registry contract, smoke, hard, compile, and documentation validation pass. hard now answers `3741` exact authored definition and statement-structure queries after `state_dict` reload, beats honest relation mph/index by `2928400` bits, and keeps random-label, shuffled-value, decoder-disabled, and wrong-query controls collapsed. product and breakthrough authorization stay `0.0` because paq8px level 2 remains cheaper by `176536` bits and the fair unlimited relation-aware scanner still solves the task.
- **2026-05-14** — deyan todorov — source-subtoken delta-update adapter added at [[tests/local_100k_source_subtoken_delta_update_adapter]] and promoted only as a narrow delta-update product. registered focused tests, registry contract, smoke, hard, compile, and documentation validation pass. hard stores arbitrary replacement bytes for `512` changed facts in a charged `147584` bit patch stream, answers updated and unchanged facts exactly after `state_dict` reload, beats full updated-adapter recompression as the same-block content-scan update baseline by `232968` delta bits, beats an undercharged mph update table by `40832` bits, and collapses random, disabled, and shuffled patch controls. total static compression authorization, static breakthrough, broad breakthrough, chat, and full nm authorization remain `0.0` because base-plus-delta total static bits lose to full recompression by `11472` bits.
- **2026-05-14** — deyan todorov — source-subtoken delta-update adapter patch encoding tightened to varint-delta offsets after the first hard run. rerun hard validation keeps exact updated and unchanged answers, state-dict reload, and control collapse at `1.0`; patch bits fall to `138104`, margin over full updated recompression rises to `242448` bits, margin over undercharged mph update rises to `50312` bits, and total static loss shrinks to `1992` bits. the result remains a narrow delta-update product only.
- **2026-05-14** — deyan todorov — source-subtoken delta-update adapter audit repairs added. the patch header tensor is now no larger than the charged header budget, focused tests now lock the hard static-negative boundary, and the registered summary reports a matched delta-patch content-scan diagnostic at `138104` bits with `0` bit margin. this prevents promotion beyond the narrow delta-update product.
- **2026-05-14** — deyan todorov — source relation mph codec added at [[tests/local_100k_source_relation_mph_codec]] as the current narrow source-authored relation-index product. hard validation answers `3741` keyed relation queries exactly from charged module state, charges `248784` relation bits, recomputes and beats the paq8px relation line by `12360` bits, beats raw-source paq content scan by `165104` bits, beats undercharged and honest mph diagnostics, and keeps self-contained paq8px baseline-win authorization at `1.0` while static breakthrough, strict 600x, broad knowledge, chat, and full nm authorization remain `0.0`.
- **2026-05-14** — deyan todorov — source relation mph codec audit repair made the paq8px relation baseline executable inside the simulation. hard validation now rebuilds the relation raw-source input, runs local `paq8px` v214 level 2, reproduces the `31619` byte archive and `261144` accounted-bit baseline, and gates product authorization on the self-contained public-baseline win.
- **2026-05-14** — deyan todorov — external relation adapter added at [[tests/local_100k_external_relation_adapter]] as the current public-corpus llm-adoptable relation adapter product. hard validation answers `6247` pinned cpython `v3.12.3` source-authored relation facts exactly from charged module state, packages the payload inside tiny transformer, recurrent, and state-space hosts, charges `413600` selected relation bits plus a `4096` bit model-package header, reaches `35.65344680851064x` strict multiplier, beats in-run paq8px source scan by `192472` bits, beats honest mph relation indexing by `3098120` bits, and keeps base-weight implicit storage, broad breakthrough, strict 600x, broad knowledge, chat, and full nm authorization at `0.0`.
- **2026-05-14** — deyan todorov — model-state knowledge pack added at [[tests/local_100k_model_state_knowledge_pack]] as the broader paper-facing bounded knowledge-pack product. hard validation answers `9754` exact cpython `v3.12.3` source, documentation, and config relation facts from charged module state, packages the payload inside tiny transformer, recurrent, and state-space hosts, charges `679400` selected relation bits plus an `8192` bit model-package header, reaches `37.6093164556962x` strict multiplier, beats in-run paq8px source scan by `705864` bits, beats zstd level 19 source scan by `1515616` bits, beats honest mph, product-key-style, rag/knn, lora-style, and model-edit storage pressure lines, passes adapter export, recompress update, rollback, and controls, and keeps base-weight implicit storage, broad breakthrough, strict 600x, broad open-domain knowledge, chat, and full nm authorization at `0.0`.

- **2026-06-02** — deyan todorov — architecture track reactivated for a one-session v0.1 (user-authorized, overriding the no-paid-compute state, on a personal runpod h200). built a clean test-gated descent-memory codebase from scratch (git branch `v0.1`, `v01/`) and ran a controlled toy comparison. the descent memory (candidate E) was made numerically stable (nlms + bounded inner lr + hard state-norm clamp) but learns at chance on mqar; the linear delta control is also at chance; a 4-layer attention model solves passkey/induction at exact_acc 1.000 (n=100, wilson 95% lower bound 0.963). seven implementation bugs were caught by the sanity gates and telemetry (gate-bias evaporation, missing cross-token binding, dead mlp init, save_ckpt rename, nlms epsilon, test-time divergence, slow selftest). run card `wiki/tests/v0_1_descent_memory_toy_results.md`, synthesis `wiki/synthesis/descent_memory_intervention.md`, bug log `wiki/mistakes/descent_memory_v0_1_bugs.md`. no 100m run. the result does not lift the retrieval wall; it narrows candidate E to "stable but not trained into retrieval by sgd." the canonical compression-lane workstream is unchanged.

- **2026-06-08** — deyan todorov — candidate F (affect-gated write) added to the paused architecture backlog as a cpu-only pre-registration, not as a reactivation of the paid track. it extends candidate E: route the descent memory's already-computed inner-loop prediction error (the surprise `||pred - v||`) into the write gain, a three-factor / neuromodulatory plasticity gate, attacking the write side that the v0.1 masked loss left unsupervised (output-side precision weighting is already maxed in v0.1 and still at chance). `affect = 0` reproduces v0.1 exactly as the control; a shuffled-surprise arm separates surprise content from a reshaped learning rate. pre-registration with controls, telemetry, and kill conditions: `wiki/tests/affect_gated_write_cpu_experiment.md`; candidate F entry added to `wiki/synthesis/substrate_requires_architectural_change.md`; bidirectional links added to `wiki/synthesis/descent_memory_intervention.md`, `wiki/mechanisms/three_factor_learning.md`, and `wiki/bridge/predictive_coding_to_training_objective.md`. no code written and no paid compute authorised; the architecture track remains paused behind the lane-research, dossier, oracle-compression, and tiny-mirror gates, and the compression-lane workstream is unchanged.

- **2026-06-10** — deyan todorov — candidate F (affect-gated write) executed on cpu per its pre-registration and CLOSED NEGATIVE. true surprise at chance under the own-history normaliser (affect 0.5 and 1.0) and under exact budget matching (per-step batch-mean normalisation, realised gain 1.000); all four sanity gates passed before every counted number. the shuffled-surprise control ignited the first sgd-trained non-chance retrieval on the descent substrate (mqar exact 0.360 / token 0.785 at 1200 steps, seed 0, n=100; memory-mediated per a pinned no-write baseline; causal max_diff 0.0) but the ignition is seed-sensitive (chance at seed 1, endogenous budget suppression) and not reproduced by uniform head-iid (token 0.098) or token-correlated (chance, both seeds) noise at matched init/data. run card frozen with the full arm table at `wiki/tests/affect_gated_write_cpu_experiment.md`; candidate F outcome and a new candidate G (stochastic write gain, backlog, pre-registration plus literature check required) recorded in `wiki/synthesis/substrate_requires_architectural_change.md`; outcome paragraph added to `wiki/synthesis/descent_memory_intervention.md`. supporting cpu context in `v01/feel_tests.md` (git `dfc1d4f`): linear matrix memory trains into partial mqar natively (exact 0.470 at 6 pairs / 400 steps; 0.520 at 8 and 0.270 at 10 at 1200 steps — an optimization cliff, not a hard wall), a 4-layer rope attention reference stays at chance through 2000 steps, the feel-bench parity question settled positive at the count-query position (1.000 vs blind chance), and feel-and-predict pretraining shows generic negative transfer (recall finetune slower than scratch, falsifying the alignment hypothesis). cpu-only session; no paid compute; the curriculum workstream and lane gates are unchanged.

## the rule

`neuroloc/wiki/PROJECT_PLAN.md` is the canonical project state. it MUST be updated whenever any of the following happens:

- a training run starts
- a training run completes (with a one-paragraph entry in the prior runs index pointing to its run card)
- a hypothesis is added or falsified
- the experimental method changes
- a new project term enters the glossary
- a prior research question is answered (move it from open research questions to a paragraph somewhere relevant, with the answer)

failing to update this file is the same class of error as failing to run the prosecutor: both are blockers for paid compute launches. an outdated `PROJECT_PLAN.md` is a bug to fix, not noise to ignore.

the update history at the bottom of this file is append-only. every change to this file gets a new entry with date, author, and a one-line description of what changed and why.

## see also

- [[OPERATING_DIRECTIVE]]
- [[neural_model_paper_spine]]
- [[oracle_compression_analysis_plan]]
- [[neural_model_lane_cellular_state_storage]]
- [[cellular_state_storage_gap_map]]
- [[neural_model_dossier_eligibility_gated_local_commit]]
- [[neural_model_symbolic_contract_eligibility_gated_local_commit]]
- [[tests/eligibility_gated_local_commit_test_material]]
- [[tests/oracle_compression_analysis_results]]
- [[oracle_compression_frontier_split]]
- [[tests/local_100k_3d_nm_mirror]]
- [[tests/local_100k_full_nm]]
- [[tests/local_100k_high_density_cell]]
- [[tests/local_100k_schema_density_cell]]
- [[tests/local_100k_unstructured_density_cell]]
- [[tests/local_100k_unknown_structure_density_probe]]
- [[tests/local_100k_learned_unknown_structure_density_cell]]
- [[tests/local_100k_content_addressed_source_codec]]
- [[tests/local_100k_paper_ready_adapter_benchmark]]
- [[tests/local_100k_margin_recompression_adapter]]
- [[tests/local_100k_weight_mantissa_payload_adapter]]
- [[tests/local_100k_indent_token_block_codec]]
- [[tests/local_100k_source_structure_block_codec]]
- [[tests/local_100k_source_structure_qa_adapter]]
- [[tests/local_100k_source_token_structure_block_codec]]
- [[tests/local_100k_source_subtoken_structure_block_codec]]
- [[tests/local_100k_source_subtoken_structure_corpus_codec]]
- [[tests/local_100k_source_subtoken_shared_dictionary_corpus_codec]]
- [[tests/local_100k_source_subtoken_global_stream_corpus_codec]]
- [[tests/local_100k_source_subtoken_delta_update_adapter]]
- [[tests/local_100k_source_subtoken_disjoint_retrieval_codec]]
- [[tests/local_100k_source_authored_relation_diagnostic]]
- [[tests/local_100k_source_dense_authored_relation_diagnostic]]
- [[tests/local_100k_source_relation_mph_codec]]
- [[tests/local_100k_external_relation_adapter]]
- [[tests/paq8px_public_context_mixing_baseline_audit]]
- [[tests/local_100k_zstd_trained_dictionary_baseline_audit]]
- [[tests/local_100k_source_subtoken_qa_adapter]]
- [[mistakes/paper_ready_adapter_source_holdout_overlap]]
- [[mistakes/dense_relation_signature_query_leakage]]
- [[mistakes/local_100k_3d_branch_rollout_overfit]]
- [[mistakes/local_100k_full_nm_soft_code_false_pass]]
- [[mistakes/local_100k_high_density_cell_strict_600x_not_met]]
- [[mistakes/schema_density_cell_structured_target_category_error]]
- [[mistakes/unstructured_exact_600x_entropy_wall]]
- [[mistakes/learned_unknown_structure_residual_table_defeat]]
- [[mistakes/source_block_codec_raw_cache_category_error]]
- [[mistakes/weight_mantissa_payload_steganographic_accounting_error]]
- [[mistakes/answer_surface_codec_static_scan_not_beaten]]
- [[mistakes/neural_coordinate_codec_random_label_memorizer]]
- [[mistakes/learned_block_codec_frontier_loses_to_standard_codec]]
- [[mistakes/public_context_mixing_baseline_missing]]
- [[schema_density_cell_boundary]]
- [[high_density_neuron_cell_related_work_pressure_matrix]]
- [[neural_model_dossier_compression_under_bit_budget_codec]]
- [[neural_model_tiny_mirror_contract_compression_under_bit_budget]]
- [[tests/compression_under_bit_budget_mirror]]
- [[tests/language_grounded_state_density_mirror]]
- [[tests/local_state_write_read_mirror]]
- [[tests/local_100k_replay_answer_mirror]]
- [[tests/local_v1_language_model]]
- [[tests/local_10k_chat_model]]
- [[tests/local_foundation_neural_model]]
- [[mistakes/local_foundation_lookup_scaffold_category_error]]
- `docs/linux_handoff_2026-05-02.md`
- [[content_routed_sparse_read_prior]]
- [[neural_model_related_work_pressure_matrix]]
- [[neural_model_lane_operation_preserving_compression]]
- [[neural_model_lane_memory_replay_imagination]]
- [[neural_model_lane_3d_world_physics]]
- [[neural_model_lane_trainability_evaluation]]
- [[neural_model_lane_project_operations]]
- [[neural_model_research_test_material_plan]]
- [[neural_model_compression_stack]]
- [[tests/hard_symbolic_nm_test_material]]
- [[substrate_requires_architectural_change]]
- [[phase1_evaluation_surface_for_neural_models]]
- [[synthetic_shared_world_bridge]]
- [[research_implications_for_neural_model_direction]]
