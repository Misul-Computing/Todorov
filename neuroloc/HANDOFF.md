# neuroloc handoff

status: current (as of 2026-06-18).

you are continuing work inside `C:\Users\deyan\Projects\todorov\neuroloc\`.

read this file before acting, but treat it as a pointer document.
the canonical persistent project state is not here. it is:

- `neuroloc/wiki/PROJECT_PLAN.md`
- `neuroloc/wiki/OPERATING_DIRECTIVE.md`
- `docs/STATUS_BOARD.md`
- `state/program_status.yaml`

## current reality

as of 2026-06-18:

- the active lane is the neural-machine architecture program (approach a:
  attention does exact recall, a recurrent state does cheap world-tracking,
  eidetic compression deferred), in cpu validation now
- the v0.1 toy (`v01/`) and the feel bench are the current cpu surface; the
  feel bench shows a located, accumulated touch sense turns an otherwise
  unsolvable task solved (blind 0.14, real 1.00, fake-touch 0.15 control)
- candidate F (affect-gated write) closed negative; candidate G (stochastic
  write gain) is the current live lead
- the six-lane neural-model research and the compression-product line are the
  research substrate that feeds the machine
- the teaching curriculum is preserved as backlog (chapter 1 review-ready,
  chapter 2 outline-only)
- paid compute is gated on funding plus a cpu-validated intervention

neuroloc roles:

- canonical architecture memory
- research corpus for the machine and for curriculum chapters (backlog)
- simulation surface for cpu validation
- repo-native llm wiki substrate

## current neuroloc inventory

- `346` wiki markdown files
- `61` mechanism articles
- `28` bridge documents
- `15` comparison articles
- `49` knowledge articles
- `55` synthesis articles
- `70` test records
- `22` mistake docs
- `94` simulation scripts
- `66` memory simulation scripts

## read order

1. `neuroloc/wiki/PROJECT_PLAN.md`
2. `neuroloc/wiki/OPERATING_DIRECTIVE.md`
3. `docs/STATUS_BOARD.md`
4. `state/program_status.yaml`
5. `neuroloc/spec/blueprint.md`
6. `pdf_curriculum/index/curriculum_status.md` (backlog)
7. this file

## what is current in neuroloc

- the six-run retrieval failure diagnosis is current
- the state-and-action-first phase-1 evaluation direction is current
- the implemented `biology_phase1` and `phase1_nm` simulation surfaces are current
- the active neural-machine architecture program (approach a) is current

## what is historical

older neuroloc documents may still describe:

- the pre-pivot paid-run plan
- fineweb or next-token-first evaluation framing
- obsolete wiki and simulation counts
- the curriculum or the compression-research phase as the active lane

when you see a conflict, trust the canonical files at the top of this handoff.

## architecture cpu surface

the architecture track is active; its current cpu-validation surface is:

- the v0.1 toy and the feel bench in `v01/`
- the implemented `biology_phase1` symbolic battery
- the model-side neural-model evaluation surface

the ranked intervention diagnosis and candidate G remain in:

- `neuroloc/wiki/synthesis/substrate_requires_architectural_change.md`

## working rules

- do not authorise or prepare a paid run without funding, a cpu-validated
  intervention, and explicit deyan approval
- update overview and handoff docs when project state changes materially
- treat overview docs as current surfaces and historical run docs as archives
- use the wiki operating directive for any wiki or state change
- preserve historical detail, but do not leave stale launch guidance at the top
  of a file

## see also

- `neuroloc/wiki/PROJECT_PLAN.md`
- `neuroloc/wiki/OPERATING_DIRECTIVE.md`
- `neuroloc/wiki/INDEX.md`
